From 8a4b58a8544ceb368d0b58e495b7567f1c6edd56 Mon Sep 17 00:00:00 2001
From: Zicklag <zicklag@katharostech.com>
Date: Fri, 19 Jun 2026 10:56:26 -0500
Subject: [PATCH] fix: update to return ints

---
 .../Chapter 4/Extension Manager.w             | 21 ++++++-----
 .../supervisor-module/Chapter 4/Kit Manager.w | 21 ++++++-----
 .../Chapter 4/Language Manager.w              | 14 ++++----
 .../Chapter 4/Pipeline Manager.w              | 15 ++++----
 .../Chapter 4/Project Bundle Manager.w        | 19 ++++++----
 .../Chapter 4/Project File Manager.w          | 19 ++++++----
 .../Chapter 4/Template Manager.w              | 15 ++++----
 .../Chapter 5/Adjectival Definition Family.w  |  9 +++--
 .../assertions-module/Chapter 5/Rule Family.w | 19 ++++++----
 .../Chapter 5/To Phrase Family.w              | 14 +++++---
 .../Chapter 8/Explicit Relations.w            |  6 ++--
 .../Chapter 8/Kind Predicates Revisited.w     |  7 ++--
 .../Chapter 8/Quasinumeric Relations.w        |  6 ++--
 .../Chapter 8/The Adjectival Predicates.w     | 12 ++++---
 .../Chapter 8/The Creation Predicates.w       | 21 +++++++----
 .../Chapter 8/The Universal Relation.w        |  3 +-
 inform7/if-module/Chapter 3/Backdrops.w       |  3 +-
 .../Chapter 3/Everywhere, Nowhere and Here.w  | 34 ++++++++++--------
 .../Chapter 3/Map Connection Relations.w      |  3 +-
 .../if-module/Chapter 3/Spatial Inferences.w  |  9 +++--
 .../if-module/Chapter 3/Spatial Relations.w   |  6 ++--
 inform7/if-module/Chapter 3/The Map.w         |  3 +-
 .../Chapter 3/Comparative Relations.w         |  3 +-
 .../Chapter 3/Either-Or Property Adjectives.w |  3 +-
 .../Chapter 3/Measurement Adjectives.w        |  3 +-
 .../Chapter 3/Same Property Relation.w        |  3 +-
 .../Chapter 3/Setting Property Relation.w     |  3 +-
 .../Chapter 3/The Provision Relation.w        |  6 ++--
 .../Chapter 4/Instance Subjects.w             |  9 +++--
 .../Chapter 4/Kind Subjects.w                 |  9 +++--
 .../Chapter 4/Relation Subjects.w             | 10 +++---
 .../Chapter 4/Variable Subjects.w             |  6 ++--
 .../Chapter 5/Property Inferences.w           |  3 +-
 .../Chapter 5/Relation Inferences.w           |  3 +-
 .../Chapter 2/Here, Nowhere and Everywhere.w  |  9 +++--
 .../building-module/Chapter 2/Inter Schemas.w |  6 ++--
 .../building-module/Chapter 2/Ramification.w  |  6 ++--
 .../Chapter 4/The Comment Construct.w         | 13 ++++---
 .../Chapter 4/The Constant Construct.w        | 26 +++++++-------
 .../Chapter 4/The Insert Construct.w          | 15 ++++----
 .../Chapter 4/The Instance Construct.w        | 27 +++++++-------
 .../Chapter 4/The Nop Construct.w             |  3 +-
 .../Chapter 4/The Package Construct.w         | 25 +++++++------
 .../Chapter 4/The PackageType Construct.w     |  7 ++--
 .../Chapter 4/The Permission Construct.w      | 31 ++++++++--------
 .../Chapter 4/The Pragma Construct.w          | 15 ++++----
 .../Chapter 4/The Primitive Construct.w       | 17 ++++-----
 .../Chapter 4/The Property Construct.w        | 16 +++++----
 .../Chapter 4/The PropertyValue Construct.w   | 28 ++++++++-------
 .../Chapter 4/The Typename Construct.w        | 30 ++++++++--------
 .../Chapter 4/The Variable Construct.w        | 18 +++++-----
 .../Chapter 5/The Assembly Construct.w        | 11 +++---
 .../Chapter 5/The Cast Construct.w            | 21 +++++------
 .../Chapter 5/The Code Construct.w            | 10 +++---
 .../Chapter 5/The Evaluation Construct.w      | 10 +++---
 .../Chapter 5/The Inv Construct.w             | 33 +++++++++--------
 .../Chapter 5/The Lab Construct.w             | 13 +++----
 .../Chapter 5/The Label Construct.w           | 13 +++----
 .../Chapter 5/The Local Construct.w           | 10 +++---
 .../Chapter 5/The Ref Construct.w             | 13 +++----
 .../Chapter 5/The Reference Construct.w       | 10 +++---
 .../Chapter 5/The Splat Construct.w           | 16 +++++----
 .../Chapter 5/The Val Construct.w             | 16 +++++----
 .../Chapter 6/The Plug Construct.w            |  9 ++---
 .../Chapter 6/The Socket Construct.w          | 11 +++---
 .../Chapter 6/The Version Construct.w         |  6 ++--
 inter/final-module/Chapter 4/Final Inform 6.w |  6 ++--
 inter/final-module/Chapter 4/Inform 6 Code.w  | 21 +++++++----
 .../Chapter 4/Inform 6 Constants.w            | 35 ++++++++++++-------
 .../Chapter 4/Inform 6 Global Variables.w     |  6 ++--
 .../final-module/Chapter 4/Inform 6 Objects.w | 24 ++++++++-----
 inter/final-module/Chapter 5/C Assembly.w     |  6 ++--
 .../final-module/Chapter 5/C Function Model.w | 15 +++++---
 .../Chapter 5/C Global Variables.w            |  6 ++--
 inter/final-module/Chapter 5/C Literals.w     | 18 ++++++----
 inter/final-module/Chapter 5/C Memory Model.w |  9 +++--
 inter/final-module/Chapter 5/C Namespace.w    |  6 ++--
 inter/final-module/Chapter 5/C Object Model.w | 18 ++++++----
 .../Chapter 5/C Program Control.w             |  3 +-
 .../Chapter 2/Kind Predicates.w               |  6 ++--
 .../Chapter 3/The Equality Relation.w         | 15 +++++---
 .../linguistics-module/Chapter 2/Adjectives.w |  3 +-
 .../linguistics-module/Chapter 2/Articles.w   |  3 +-
 .../Chapter 2/Determiners and Quantifiers.w   |  3 +-
 services/linguistics-module/Chapter 2/Nouns.w |  3 +-
 .../linguistics-module/Chapter 2/Pronouns.w   |  3 +-
 .../Chapter 3/Prepositions.w                  |  3 +-
 services/linguistics-module/Chapter 3/Verbs.w |  6 ++--
 88 files changed, 630 insertions(+), 422 deletions(-)

diff --git a/inbuild/supervisor-module/Chapter 4/Extension Manager.w b/inbuild/supervisor-module/Chapter 4/Extension Manager.w
index a34cc654b..f6ff5b793 100644
--- a/inbuild/supervisor-module/Chapter 4/Extension Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Extension Manager.w	
@@ -27,8 +27,9 @@ void ExtensionManager::start(void) {
 	METHOD_ADD(extension_genre, GENRE_BUILDING_SOON_MTID, ExtensionManager::building_soon);
 }
 
-void ExtensionManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int ExtensionManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%X", work);
+	return 0;
 }
 
 @ Extensions live in their namesake subdirectory of a nest:
@@ -84,9 +85,9 @@ also to scan properly -- which means the top line of the file has to be right.
 So we'll open it and look.
 
 =
-void ExtensionManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int ExtensionManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == TRUE) return;
+	if (directory_status == TRUE) return 0;
 	if (Str::eq_insensitive(ext, I"i7x")) {
 		filename *F = Filenames::from_text(arg);
 		*C = ExtensionManager::claim_file_as_copy(F, NULL);
@@ -117,9 +118,9 @@ extensions. Similarly, another bad emacs habit is to write hidden symlinks,
 with leafnames beginning |.|, so we reject those too.
 
 =
-void ExtensionManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int ExtensionManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
-	if ((req->work->genre) && (req->work->genre != extension_genre)) return;
+	if ((req->work->genre) && (req->work->genre != extension_genre)) return 0;
 	pathname *P = ExtensionManager::path_within_nest(N);
 	if (Str::len(req->work->author_name) > 0) {
 		linked_list *L = Directories::listing(P);
@@ -176,7 +177,7 @@ Now the task is to copy an extension into place in a nest. This is easy,
 since an extension is a single file; to sync, we just overwrite.
 
 =
-void ExtensionManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int ExtensionManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	pathname *E = ExtensionManager::path_within_nest(N);
 	TEMPORARY_TEXT(leaf)
@@ -187,7 +188,7 @@ void ExtensionManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild
 	DISCARD_TEXT(leaf)
 
 	if (TextFiles::exists(F)) {
-		if (syncing == FALSE) { Copies::overwrite_error(C, N); return; }
+		if (syncing == FALSE) { Copies::overwrite_error(C, N); return 0; }
 	} else {
 		if (meth->methodology == DRY_RUN_METHODOLOGY) {
 			TEMPORARY_TEXT(command)
@@ -225,9 +226,10 @@ the current VM settings.
 We therefore generate the build graph only on demand.
 
 =
-void ExtensionManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
+int ExtensionManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
 	ExtensionManager::ensure_graphed(C);
 	*V = C->vertex;
+	return 0;
 }
 
 void ExtensionManager::ensure_graphed(inbuild_copy *C) {
@@ -240,6 +242,7 @@ void ExtensionManager::ensure_graphed(inbuild_copy *C) {
 @h Source text.
 
 =
-void ExtensionManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
+int ExtensionManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
 	Extensions::read_source_text_for(ExtensionManager::from_copy(C));
+	return 0;
 }
diff --git a/inbuild/supervisor-module/Chapter 4/Kit Manager.w b/inbuild/supervisor-module/Chapter 4/Kit Manager.w
index 302e77072..094349f0a 100644
--- a/inbuild/supervisor-module/Chapter 4/Kit Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Kit Manager.w	
@@ -24,8 +24,9 @@ void KitManager::start(void) {
 	METHOD_ADD(kit_genre, GENRE_BUILDING_SOON_MTID, KitManager::building_soon);
 }
 
-void KitManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int KitManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Kits live in the |Inter| subdirectory of a nest:
@@ -78,9 +79,9 @@ A kit needs to be a directory whose name ends in |Kit|, perhaps with a semver
 appended to it, and which contains a valid metadata file.
 
 =
-void KitManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int KitManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == FALSE) return;
+	if (directory_status == FALSE) return 0;
 	if (KitManager::name_len(arg) > 0) {
 		pathname *P = Pathnames::from_text(arg);
 		*C = KitManager::claim_folder_as_copy(P, NULL);
@@ -124,9 +125,9 @@ Here we look through a nest to find all kits matching the supplied
 requirements.
 
 =
-void KitManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int KitManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
-	if ((req->work->genre) && (req->work->genre != kit_genre)) return;
+	if ((req->work->genre) && (req->work->genre != kit_genre)) return 0;
 	pathname *P = KitManager::path_within_nest(N);
 	linked_list *L = Directories::listing(P);
 	text_stream *entry;
@@ -155,12 +156,12 @@ pathname *KitManager::pathname_in_nest(inbuild_nest *N, inbuild_edition *E) {
 	return P;
 }
 
-void KitManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int KitManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	pathname *dest_kit = KitManager::pathname_in_nest(N, C->edition);
 	filename *dest_kit_metadata = Filenames::in(dest_kit, I"kit_metadata.json");
 	if (TextFiles::exists(dest_kit_metadata)) {
-		if (syncing == FALSE) { Copies::overwrite_error(C, N); return; }
+		if (syncing == FALSE) { Copies::overwrite_error(C, N); return 0; }
 	} else {
 		if (meth->methodology == DRY_RUN_METHODOLOGY) {
 			TEMPORARY_TEXT(command)
@@ -189,10 +190,12 @@ void KitManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest
 @h Build graph.
 
 =
-void KitManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
+int KitManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
 	*V = C->vertex;
+	return 0;
 }
 
-void KitManager::construct_graph(inbuild_genre *G, inbuild_copy *C) {
+int KitManager::construct_graph(inbuild_genre *G, inbuild_copy *C) {
 	Kits::construct_graph(KitManager::from_copy(C));
+	return 0;
 }
diff --git a/inbuild/supervisor-module/Chapter 4/Language Manager.w b/inbuild/supervisor-module/Chapter 4/Language Manager.w
index 0712a9e45..1c18a9075 100644
--- a/inbuild/supervisor-module/Chapter 4/Language Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Language Manager.w	
@@ -19,8 +19,9 @@ void LanguageManager::start(void) {
 	METHOD_ADD(language_genre, GENRE_COPY_TO_NEST_MTID, LanguageManager::copy_to_nest);
 }
 
-void LanguageManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int LanguageManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Languages live in the |Inter| subdirectory of a nest:
@@ -73,9 +74,9 @@ A language needs to be a directory whose name ends in |Language|, and which cont
 a valid metadata file. The name should be in English text, without accents.
 
 =
-void LanguageManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int LanguageManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == FALSE) return;
+	if (directory_status == FALSE) return 0;
 	pathname *P = Pathnames::from_text(arg);
 	text_stream *name = Pathnames::directory_name(P);
 	int acceptable = TRUE;
@@ -105,7 +106,7 @@ Here we look through a nest to find all languages matching the supplied
 requirements.
 
 =
-void LanguageManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int LanguageManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
 	pathname *P = LanguageManager::path_within_nest(N);
 	linked_list *L = Directories::listing(P);
@@ -120,6 +121,7 @@ void LanguageManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 			}
 		}
 	}
+	return 0;
 }
 
 @h Copying.
@@ -135,12 +137,12 @@ pathname *LanguageManager::pathname_in_nest(inbuild_nest *N, inbuild_edition *E)
 	return P;
 }
 
-void LanguageManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int LanguageManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	pathname *dest_language = LanguageManager::pathname_in_nest(N, C->edition);
 	filename *dest_language_metadata = Filenames::in(dest_language, I"about.txt");
 	if (TextFiles::exists(dest_language_metadata)) {
-		if (syncing == FALSE) { Copies::overwrite_error(C, N); return; }
+		if (syncing == FALSE) { Copies::overwrite_error(C, N); return 0; }
 	} else {
 		if (meth->methodology == DRY_RUN_METHODOLOGY) {
 			TEMPORARY_TEXT(command)
diff --git a/inbuild/supervisor-module/Chapter 4/Pipeline Manager.w b/inbuild/supervisor-module/Chapter 4/Pipeline Manager.w
index d765195e2..2c5f99c0a 100644
--- a/inbuild/supervisor-module/Chapter 4/Pipeline Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Pipeline Manager.w	
@@ -18,8 +18,9 @@ void PipelineManager::start(void) {
 	METHOD_ADD(pipeline_genre, GENRE_COPY_TO_NEST_MTID, PipelineManager::copy_to_nest);
 }
 
-void PipelineManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int PipelineManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Pipelines live in their namesake subdirectory of a nest:
@@ -58,9 +59,9 @@ not a file, false if we know the reverse, and otherwise not applicable.
 An pipeline, for us, simply needs to be a file with extension |.interpipeline|.
 
 =
-void PipelineManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int PipelineManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == TRUE) return;
+	if (directory_status == TRUE) return 0;
 	if (Str::eq_insensitive(ext, I"interpipeline")) {
 		filename *F = Filenames::from_text(arg);
 		*C = PipelineManager::claim_file_as_copy(F, NULL, NULL);
@@ -84,9 +85,9 @@ Here we look through a nest to find all pipelines matching the supplied
 requirements.
 
 =
-void PipelineManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int PipelineManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
-	if ((req->work->genre) && (req->work->genre != pipeline_genre)) return;
+	if ((req->work->genre) && (req->work->genre != pipeline_genre)) return 0;
 	pathname *P = PipelineManager::path_within_nest(N);
 	linked_list *L = Directories::listing(P);
 	text_stream *entry;
@@ -115,12 +116,12 @@ filename *PipelineManager::filename_in_nest(inbuild_nest *N, inbuild_edition *E)
 	return F;
 }
 
-void PipelineManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int PipelineManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	filename *F = PipelineManager::filename_in_nest(N, C->edition);
 
 	if (TextFiles::exists(F)) {
-		if (syncing == FALSE) { Copies::overwrite_error(C, N); return; }
+		if (syncing == FALSE) { Copies::overwrite_error(C, N); return 0; }
 	} else {
 		if (meth->methodology == DRY_RUN_METHODOLOGY) {
 			TEMPORARY_TEXT(command)
diff --git a/inbuild/supervisor-module/Chapter 4/Project Bundle Manager.w b/inbuild/supervisor-module/Chapter 4/Project Bundle Manager.w
index 51b14690d..e146d5b4e 100644
--- a/inbuild/supervisor-module/Chapter 4/Project Bundle Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Project Bundle Manager.w	
@@ -28,8 +28,9 @@ void ProjectBundleManager::start(void) {
 	METHOD_ADD(project_bundle_genre, GENRE_BUILDING_SOON_MTID, ProjectBundleManager::building_soon);
 }
 
-void ProjectBundleManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int ProjectBundleManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Project copies are annotated with a structure called an |inform_project|,
@@ -61,9 +62,9 @@ not a file, false if we know the reverse, and otherwise not applicable.
 A project needs to be a directory whose name ends in |.inform|.
 
 =
-void ProjectBundleManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int ProjectBundleManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == FALSE) return;
+	if (directory_status == FALSE) return 0;
 	if (Str::eq_insensitive(ext, I"inform")) {
 		pathname *P = Pathnames::from_text(arg);
 		*C = ProjectBundleManager::claim_folder_as_copy(P);
@@ -82,8 +83,9 @@ Here we look through a nest to find all projects matching the supplied
 requirements; though in fact... projects are not nesting birds.
 
 =
-void ProjectBundleManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int ProjectBundleManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
+	return 0;
 }
 
 @h Copying.
@@ -91,10 +93,11 @@ Now the task is to copy a project into place in a nest; or would be, if only
 projects lived there.
 
 =
-void ProjectBundleManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int ProjectBundleManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	Errors::with_text("projects (which is what '%S' is) cannot be copied to nests",
 		C->edition->work->title);
+	return 0;
 }
 
 @h Build graph.
@@ -102,15 +105,17 @@ As with extensions, the graph for a project is made only on demand, because to m
 it would mean fully parsing and partially syntax-analysing its source text.
 
 =
-void ProjectBundleManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
+int ProjectBundleManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
 	inform_project *project = ProjectBundleManager::from_copy(C);
 	Projects::construct_graph(project);
 	*V = project->chosen_build_target;
+	return 0;
 }
 
 @h Source text.
 
 =
-void ProjectBundleManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
+int ProjectBundleManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
 	Projects::read_source_text_for(ProjectBundleManager::from_copy(C));
+	return 0;
 }
diff --git a/inbuild/supervisor-module/Chapter 4/Project File Manager.w b/inbuild/supervisor-module/Chapter 4/Project File Manager.w
index ab5bd7f39..d8269c97f 100644
--- a/inbuild/supervisor-module/Chapter 4/Project File Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Project File Manager.w	
@@ -26,8 +26,9 @@ void ProjectFileManager::start(void) {
 	METHOD_ADD(project_file_genre, GENRE_BUILDING_SOON_MTID, ProjectFileManager::building_soon);
 }
 
-void ProjectFileManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int ProjectFileManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Project copies are annotated with a structure called an |inform_project|,
@@ -60,9 +61,9 @@ A project file needs to be a plain text file whose name ends in |.txt|, |.ni|
 or |.i7|.
 
 =
-void ProjectFileManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int ProjectFileManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == TRUE) return;
+	if (directory_status == TRUE) return 0;
 	if ((Str::eq_insensitive(ext, I"txt")) ||
 		(Str::eq_insensitive(ext, I"ni")) ||
 		(Str::eq_insensitive(ext, I"i7"))) {
@@ -84,8 +85,9 @@ Here we look through a nest to find all projects matching the supplied
 requirements; though in fact... projects are not nesting birds.
 
 =
-void ProjectFileManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int ProjectFileManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
+	return 0;
 }
 
 @h Copying.
@@ -93,10 +95,11 @@ Now the task is to copy a project into place in a nest; or would be, if only
 projects lived there.
 
 =
-void ProjectFileManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int ProjectFileManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	Errors::with_text("projects (which is what '%S' is) cannot be copied to nests",
 		C->edition->work->title);
+	return 0;
 }
 
 @h Build graph.
@@ -104,15 +107,17 @@ As with extensions, the graph for a project is made only on demand, because to m
 it would mean fully parsing and partially syntax-analysing its source text.
 
 =
-void ProjectFileManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
+int ProjectFileManager::building_soon(inbuild_genre *gen, inbuild_copy *C, build_vertex **V) {
 	inform_project *project = ProjectFileManager::from_copy(C);
 	Projects::construct_graph(project);
 	*V = project->chosen_build_target;
+	return 0;
 }
 
 @h Source text.
 
 =
-void ProjectFileManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
+int ProjectFileManager::read_source_text_for(inbuild_genre *G, inbuild_copy *C) {
 	Projects::read_source_text_for(ProjectFileManager::from_copy(C));
+	return 0;
 }
diff --git a/inbuild/supervisor-module/Chapter 4/Template Manager.w b/inbuild/supervisor-module/Chapter 4/Template Manager.w
index 481e3107d..7bc4eac21 100644
--- a/inbuild/supervisor-module/Chapter 4/Template Manager.w	
+++ b/inbuild/supervisor-module/Chapter 4/Template Manager.w	
@@ -19,8 +19,9 @@ void TemplateManager::start(void) {
 	METHOD_ADD(template_genre, GENRE_COPY_TO_NEST_MTID, TemplateManager::copy_to_nest);
 }
 
-void TemplateManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
+int TemplateManager::write_work(inbuild_genre *gen, OUTPUT_STREAM, inbuild_work *work) {
 	WRITE("%S", work->title);
+	return 0;
 }
 
 @ Templates live in the |Templates| subdirectory of a nest:
@@ -61,9 +62,9 @@ Templates are slightly hard to recognise in isolation, but they contain
 either a manifest file, or else an |index.html|, so that will have to do.
 
 =
-void TemplateManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
+int TemplateManager::claim_as_copy(inbuild_genre *gen, inbuild_copy **C,
 	text_stream *arg, text_stream *ext, int directory_status) {
-	if (directory_status == FALSE) return;
+	if (directory_status == FALSE) return 0;
 	pathname *P = Pathnames::from_text(arg);
 	*C = TemplateManager::claim_folder_as_copy(P, NULL);
 }
@@ -81,9 +82,9 @@ Here we look through a nest to find all kits matching the supplied
 requirements.
 
 =
-void TemplateManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
+int TemplateManager::search_nest_for(inbuild_genre *gen, inbuild_nest *N,
 	inbuild_requirement *req, linked_list *search_results) {
-	if ((req->work->genre) && (req->work->genre != template_genre)) return;
+	if ((req->work->genre) && (req->work->genre != template_genre)) return 0;
 	pathname *P = TemplateManager::path_within_nest(N);
 	linked_list *L = Directories::listing(P);
 	text_stream *entry;
@@ -112,13 +113,13 @@ pathname *TemplateManager::pathname_in_nest(inbuild_nest *N, inbuild_edition *E)
 	return P;
 }
 
-void TemplateManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
+int TemplateManager::copy_to_nest(inbuild_genre *gen, inbuild_copy *C, inbuild_nest *N,
 	int syncing, build_methodology *meth) {
 	pathname *P = TemplateManager::pathname_in_nest(N, C->edition);
 	filename *canary1 = Filenames::in(P, I"(manifest).txt");
 	filename *canary2 = Filenames::in(P, I"index.html");
 	if ((TextFiles::exists(canary1)) || (TextFiles::exists(canary2))) {
-		if (syncing == FALSE) { Copies::overwrite_error(C, N); return; }
+		if (syncing == FALSE) { Copies::overwrite_error(C, N); return 0; }
 	} else {
 		if (meth->methodology == DRY_RUN_METHODOLOGY) {
 			TEMPORARY_TEXT(command)
diff --git a/inform7/assertions-module/Chapter 5/Adjectival Definition Family.w b/inform7/assertions-module/Chapter 5/Adjectival Definition Family.w
index a475d5a89..12ad8d83b 100644
--- a/inform7/assertions-module/Chapter 5/Adjectival Definition Family.w	
+++ b/inform7/assertions-module/Chapter 5/Adjectival Definition Family.w	
@@ -45,7 +45,7 @@ of its own.
 	definition
 
 @ =
-void AdjectivalDefinitionFamily::identify(imperative_defn_family *self, imperative_defn *id) {
+int AdjectivalDefinitionFamily::identify(imperative_defn_family *self, imperative_defn *id) {
 	wording W = Node::get_text(id->at);
 	if (<definition-preamble>(W)) {
 		id->family = adjectival_idf;
@@ -56,6 +56,7 @@ void AdjectivalDefinitionFamily::identify(imperative_defn_family *self, imperati
 		}
 		AdjectivalDefinitionFamily::look_for_headers(id->at);
 	}
+	return 0;
 }
 
 @ Since the "Definition:" node might have no code under it (because the code
@@ -73,7 +74,7 @@ perhaps also with a calling -- consider the example "Definition: A
 container (called the sack) is capacious if...".
 
 =
-void AdjectivalDefinitionFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
+int AdjectivalDefinitionFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
 	id_body *body = id->body_of_defn;
 
 	IDTypeData::set_mor(&(body->type_data), DECIDES_CONDITION_MOR, NULL);
@@ -83,12 +84,13 @@ void AdjectivalDefinitionFamily::given_body(imperative_defn_family *self, impera
 	AdjectivesByPhrase::define_adjective_by_phrase(id->at, body, &CALLW, &K);
 	Frames::enable_it(&(body->compilation_data.id_stack_frame), CALLW, K);
 
+	return 0;
 }
 
 @ The code body for any definition is compiled here:
 
 =
-void AdjectivalDefinitionFamily::compile(imperative_defn_family *self,
+int AdjectivalDefinitionFamily::compile(imperative_defn_family *self,
 	int *total_phrases_compiled, int total_phrases_to_compile) {
 	imperative_defn *id;
 	LOOP_OVER(id, imperative_defn)
@@ -98,6 +100,7 @@ void AdjectivalDefinitionFamily::compile(imperative_defn_family *self,
 				total_phrases_to_compile, NULL, NULL);
 		}
 	RTAdjectives::compile();
+	return 0;
 }
 
 @h Implementation details.
diff --git a/inform7/assertions-module/Chapter 5/Rule Family.w b/inform7/assertions-module/Chapter 5/Rule Family.w
index 1b70b8906..a1b84b3db 100644
--- a/inform7/assertions-module/Chapter 5/Rule Family.w	
+++ b/inform7/assertions-module/Chapter 5/Rule Family.w	
@@ -131,7 +131,7 @@ happen. (That's why it is important that we are the last family to claim.)
 @ Forms 1 and 2 give a rule name; forms 2 and 3 say which rulebook it goes into.
 
 =
-void RuleFamily::identify(imperative_defn_family *self, imperative_defn *id) {
+int RuleFamily::identify(imperative_defn_family *self, imperative_defn *id) {
 	wording W = Node::get_text(id->at);
 	if (<rule-preamble>(W)) {
 		int form = <<r>>;
@@ -158,6 +158,7 @@ void RuleFamily::identify(imperative_defn_family *self, imperative_defn *id) {
 
 		PluginCalls::new_rule_defn_notify(id, rfd);
 	}
+	return 0;
 }
 
 @h Assessment.
@@ -220,7 +221,7 @@ then calling |Rulebooks::match| produces a detailed rundown of its findings,
 which are too elaborate to pass back in a simple pointer.
 
 =
-void RuleFamily::assess(imperative_defn_family *self, imperative_defn *id) {
+int RuleFamily::assess(imperative_defn_family *self, imperative_defn *id) {
 	rule_family_data *rfd = RETRIEVE_POINTER_rule_family_data(id->family_specific_data);
 	if (rfd->not_in_rulebook == FALSE) {
 		wording W = rfd->usage_preamble;
@@ -242,6 +243,7 @@ void RuleFamily::assess(imperative_defn_family *self, imperative_defn *id) {
 		}
 		rfd->pruned_stem = W;
 	}
+	return 0;
 }
 
 @ This is a super-pedantic problem message, and might cause problems in languages
@@ -347,7 +349,7 @@ then we have a predeclared rule called "avoid water rule" already, so we
 connect this existing one to the phrase.
 
 =
-void RuleFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
+int RuleFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
 	rule_family_data *rfd = RETRIEVE_POINTER_rule_family_data(id->family_specific_data);
 	rule *R = NULL;
 	@<Set R to a corresponding rule structure@>;
@@ -356,6 +358,7 @@ void RuleFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
 	IDTypeData::set_mor(&(id->body_of_defn->type_data),
 		DECIDES_NOTHING_AND_RETURNS_MOR, NULL);
 	if (rfd->not_in_rulebook) rfd->permit_all_outcomes = TRUE;
+	return 0;
 }
 
 @<Set R to a corresponding rule structure@> =
@@ -418,7 +421,7 @@ for how sentences specifying this are parsed.
 @e TRAVERSE_FOR_RULE_FILING_SMFT
 
 =
-void RuleFamily::assessment_complete(imperative_defn_family *self) {
+int RuleFamily::assessment_complete(imperative_defn_family *self) {
 	imperative_defn *id;
 	LOOP_OVER(id, imperative_defn)
 		if (id->family == rule_idf) {
@@ -428,7 +431,7 @@ void RuleFamily::assessment_complete(imperative_defn_family *self) {
 
 	int initial_problem_count = problem_count;
 	RuleBookings::make_automatic_placements();
-	if (initial_problem_count < problem_count) return;
+	if (initial_problem_count < problem_count) return 0;
 
 	SyntaxTree::traverse(Task::syntax_tree(), RuleFamily::visit_to_parse_placements);
 }
@@ -449,7 +452,7 @@ int NAP_problem_explained = FALSE; /* pertains to Named Action Patterns */
 int issuing_ANL_problem = FALSE; /* pertains to Action Name Lists */
 int defective_ANL_clauses = 0; /* ditto */
 
-void RuleFamily::to_rcd(imperative_defn_family *self, imperative_defn *id,
+int RuleFamily::to_rcd(imperative_defn_family *self, imperative_defn *id,
 	id_runtime_context_data *rcd) {
 	rule_family_data *rfd = RETRIEVE_POINTER_rule_family_data(id->family_specific_data);
 	if (rfd->not_in_rulebook == FALSE) {
@@ -460,6 +463,7 @@ void RuleFamily::to_rcd(imperative_defn_family *self, imperative_defn *id,
 				Wordings::from(rfd->whenwhile, Wordings::first_wn(rfd->whenwhile) + 1);
 		if (rfd->during_spec) Scenes::set_rcd_spec(rcd, rfd->during_spec);
 	}
+	return 0;
 }
 
 @ Here we try the text first without its when clause, then with, and accept
@@ -805,7 +809,7 @@ The actual compilation of rules is done elsewhere; here we simply make sure
 that rule bodies won't be accidentally compiled as if they were phrase bodies.
 
 =
-void RuleFamily::compile(imperative_defn_family *self,
+int RuleFamily::compile(imperative_defn_family *self,
 	int *total_phrases_compiled, int total_phrases_to_compile) {
 	rule *R;
 	LOOP_OVER(R, rule) {
@@ -814,6 +818,7 @@ void RuleFamily::compile(imperative_defn_family *self,
 				at_least_one_compiled_form_needed = FALSE;
 		Rules::check_constraints_are_typesafe(R);
 	}
+	return 0;
 }
 
 @h Miscellaneous access functions.
diff --git a/inform7/assertions-module/Chapter 5/To Phrase Family.w b/inform7/assertions-module/Chapter 5/To Phrase Family.w
index 5d49a9976..3c5daf1e3 100644
--- a/inform7/assertions-module/Chapter 5/To Phrase Family.w	
+++ b/inform7/assertions-module/Chapter 5/To Phrase Family.w	
@@ -89,7 +89,7 @@ must begin with "to" and contain at least one other word.
 	==> { 4, - };
 
 @ =
-void ToPhraseFamily::claim(imperative_defn_family *self, imperative_defn *id) {
+int ToPhraseFamily::claim(imperative_defn_family *self, imperative_defn *id) {
 	wording W = Node::get_text(id->at);
 	if (<to-phrase-preamble>(W)) {
 		id->family = to_phrase_idf;
@@ -117,6 +117,7 @@ void ToPhraseFamily::claim(imperative_defn_family *self, imperative_defn *id) {
 		tfd->prototype_text = PW;
 		id->log_text = PW;
 	}
+	return 0;
 }
 
 @h Assessment.
@@ -131,7 +132,7 @@ Now we take a closer look at the wording, and react to these two in particular:
 
 @ =
 int no_now_phrases = 0;
-void ToPhraseFamily::assess(imperative_defn_family *self, imperative_defn *id) {
+int ToPhraseFamily::assess(imperative_defn_family *self, imperative_defn *id) {
 	to_family_data *tfd = RETRIEVE_POINTER_to_family_data(id->family_specific_data);
 
 	if (Wordings::nonempty(tfd->constant_name)) @<The preamble parses to a named To phrase@>;
@@ -145,6 +146,7 @@ void ToPhraseFamily::assess(imperative_defn_family *self, imperative_defn *id) {
 			"abilities, the idea is to create new relations.)");
 	}
 	if (<begin-phrase-preamble>(W)) tfd->to_begin = TRUE;
+	return 0;
 }
 
 @ Phrases are allowed to have indefinite kinds provided they are used only as
@@ -190,7 +192,7 @@ within rulebooks. That's a similar issue, but addressed differently elsewhere.
 =
 imperative_defn *first_in_logical_order = NULL;
 
-void ToPhraseFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
+int ToPhraseFamily::given_body(imperative_defn_family *self, imperative_defn *id) {
 	id_body *body = id->body_of_defn;
 	CompileImperativeDefn::prepare_for_requests(body);
 
@@ -216,6 +218,7 @@ void ToPhraseFamily::given_body(imperative_defn_family *self, imperative_defn *i
 			ToPhraseFamily::set_next(id, current_id);
 		}
 	}
+	return 0;
 }
 
 imperative_defn *ToPhraseFamily::get_next(imperative_defn *id) {
@@ -282,7 +285,7 @@ precedence list. We now both enter them into the excerpt parser, and also
 number them sequentially from 0:
 
 =
-void ToPhraseFamily::register(imperative_defn_family *self) {
+int ToPhraseFamily::register(imperative_defn_family *self) {
 	int c = 0;
 	for (imperative_defn *id = first_in_logical_order; id; id = ToPhraseFamily::get_next(id)) {
 		current_sentence = id->at;
@@ -290,13 +293,14 @@ void ToPhraseFamily::register(imperative_defn_family *self) {
 		to_family_data *tfd = RETRIEVE_POINTER_to_family_data(id->family_specific_data);
 		tfd->sequence_count = c++;
 	}
+	return 0;
 }
 
 @h Compilation.
 We actually don't compile anything here: we just make "requests" to do so.
 
 =
-void ToPhraseFamily::compile(imperative_defn_family *self,
+int ToPhraseFamily::compile(imperative_defn_family *self,
 	int *total_phrases_compiled, int total_phrases_to_compile) {
 	@<Mark To... phrases which have definite kinds for future compilation@>;
 	@<Throw problems for phrases with return kinds too vaguely defined@>;
diff --git a/inform7/assertions-module/Chapter 8/Explicit Relations.w b/inform7/assertions-module/Chapter 8/Explicit Relations.w
index e4821e4a6..890f2ca64 100644
--- a/inform7/assertions-module/Chapter 8/Explicit Relations.w	
+++ b/inform7/assertions-module/Chapter 8/Explicit Relations.w	
@@ -234,7 +234,7 @@ int ExplicitRelations::schema(bp_family *self, int task, binary_predicate *bp, a
 int ExplicitRelations::describe_for_problems(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
 	return FALSE;
 }
-void ExplicitRelations::describe_for_index(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
+int ExplicitRelations::describe_for_index(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
 	switch (ExplicitRelations::get_form_of_relation(bp)) {
 		case Relation_OtoO: WRITE("one-to-one"); break;
 		case Relation_OtoV: WRITE("one-to-various"); break;
@@ -244,7 +244,9 @@ void ExplicitRelations::describe_for_index(bp_family *self, OUTPUT_STREAM, binar
 		case Relation_Sym_VtoV: WRITE("various-to-each-other"); break;
 		case Relation_Equiv: WRITE("in groups"); break;
 	}
+	return 0;
 }
-void ExplicitRelations::REL_br_describe_briefly(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
+int ExplicitRelations::REL_br_describe_briefly(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
 	WRITE("defined");
+	return 0;
 }
diff --git a/inform7/assertions-module/Chapter 8/Kind Predicates Revisited.w b/inform7/assertions-module/Chapter 8/Kind Predicates Revisited.w
index 8c255043a..31d268d72 100644
--- a/inform7/assertions-module/Chapter 8/Kind Predicates Revisited.w	
+++ b/inform7/assertions-module/Chapter 8/Kind Predicates Revisited.w	
@@ -51,13 +51,13 @@ is not a door." will fall through here, but it isn't exactly an everyday
 sentence.
 
 =
-void KindPredicatesRevisited::assert(up_family *self, unary_predicate *up,
+int KindPredicatesRevisited::assert(up_family *self, unary_predicate *up,
 	int now_negated, pcalc_prop *pl) {
 	if (now_negated) {
 		StandardProblems::sentence_problem(Task::syntax_tree(), _p_(PM_CantAssertNonKind),
 			"that seems to say what kind something doesn't have",
 			"which is too vague. You must say what kind it does have.");
-		return;
+		return 0;
 	}
 	inference_subject *subj = Assert::subject_of_term(pl->terms[0]);
 	instance *ox = InstanceSubjects::to_object_instance(subj);
@@ -78,7 +78,7 @@ the I6 |ofclass| operator.
 
 =
 int suppress_C14CantChangeKind = FALSE;
-void KindPredicatesRevisited::get_schema(up_family *self, int task, unary_predicate *up,
+int KindPredicatesRevisited::get_schema(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	switch(task) {
 		case TEST_ATOM_TASK:
@@ -118,4 +118,5 @@ void KindPredicatesRevisited::get_schema(up_family *self, int task, unary_predic
 			} else Calculus::Schemas::modify(asch->schema, " ");
 			break;
 	}
+	return 0;
 }
diff --git a/inform7/assertions-module/Chapter 8/Quasinumeric Relations.w b/inform7/assertions-module/Chapter 8/Quasinumeric Relations.w
index c707f9467..884f4fcfd 100644
--- a/inform7/assertions-module/Chapter 8/Quasinumeric Relations.w	
+++ b/inform7/assertions-module/Chapter 8/Quasinumeric Relations.w	
@@ -40,7 +40,7 @@ void Calculus::QuasinumericRelations::start(void) {
 These relations are all hard-wired in.
 
 =
-void Calculus::QuasinumericRelations::stock(bp_family *self, int n) {
+int Calculus::QuasinumericRelations::stock(bp_family *self, int n) {
 	if (n == 1) {
 		bp_term_details number_term = BPTerms::new(KindSubjects::from_kind(K_number));
 		R_numerically_greater_than =
@@ -72,6 +72,7 @@ void Calculus::QuasinumericRelations::stock(bp_family *self, int n) {
 		BinaryPredicates::set_index_details(R_numerically_less_than_or_equal_to,
 			"arithmetic value", "arithmetic value");
 	}
+	return 0;
 }
 
 @h Typechecking.
@@ -172,6 +173,7 @@ int Calculus::QuasinumericRelations::schema(bp_family *self, int task, binary_pr
 int Calculus::QuasinumericRelations::describe_for_problems(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
 	return FALSE;
 }
-void Calculus::QuasinumericRelations::describe_for_index(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
+int Calculus::QuasinumericRelations::describe_for_index(bp_family *self, OUTPUT_STREAM, binary_predicate *bp) {
 	WRITE("numeric");
+	return 0;
 }
diff --git a/inform7/assertions-module/Chapter 8/The Adjectival Predicates.w b/inform7/assertions-module/Chapter 8/The Adjectival Predicates.w
index 2bde4e3a6..175216548 100644
--- a/inform7/assertions-module/Chapter 8/The Adjectival Predicates.w	
+++ b/inform7/assertions-module/Chapter 8/The Adjectival Predicates.w	
@@ -38,17 +38,19 @@ pcalc_prop *AdjectivalPredicates::new_atom_on_x(adjective *adj, int negated) {
 	return AdjectivalPredicates::new_atom(adj, negated, Terms::new_variable(0));
 }
 
-void AdjectivalPredicates::log(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int AdjectivalPredicates::log(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	if (AdjectivalPredicates::parity(up) == FALSE) WRITE("not-");
 	wording W = Adjectives::get_nominative_singular(AdjectivalPredicates::to_adjective(up));
 	WRITE("%W", W);
+	return 0;
 }
 
-void AdjectivalPredicates::infer_kind(up_family *self, unary_predicate *up, kind **K) {
+int AdjectivalPredicates::infer_kind(up_family *self, unary_predicate *up, kind **K) {
 	adjective *adj = AdjectivalPredicates::to_adjective(up);
 	adjective_meaning *am = AdjectiveAmbiguity::first_meaning(adj);
 	kind *D = AdjectiveMeaningDomains::get_kind(am);
 	if (D) *K = D;
+	return 0;
 }
 
 int AdjectivalPredicates::typecheck(up_family *self, unary_predicate *up,
@@ -72,7 +74,7 @@ not have survived type-checking. But only some adjectives can be asserted;
 success flag.
 
 =
-void AdjectivalPredicates::assert(up_family *self, unary_predicate *up,
+int AdjectivalPredicates::assert(up_family *self, unary_predicate *up,
 	int now_negated, pcalc_prop *pl) {
 	adjective *adj = AdjectivalPredicates::to_adjective(up);
 	int parity = (now_negated)?FALSE:TRUE, found;
@@ -99,6 +101,7 @@ void AdjectivalPredicates::assert(up_family *self, unary_predicate *up,
 	else found = AdjectiveAmbiguity::assert(adj, domain_of_definition, NULL, ots, parity);
 
 	if (found == FALSE) Assert::issue_couldnt_problem(adj, parity);
+	return 0;
 }
 
 int AdjectivalPredicates::testable(up_family *self, unary_predicate *up) {
@@ -125,13 +128,14 @@ int AdjectivalPredicates::test(up_family *self, unary_predicate *up,
 	return FALSE;
 }
 
-void AdjectivalPredicates::get_schema(up_family *self, int task, unary_predicate *up,
+int AdjectivalPredicates::get_schema(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	adjective *adj = AdjectivalPredicates::to_adjective(up);
 
 	if (AdjectivalPredicates::parity(up) == FALSE) asch->negate_schema = TRUE;
 
 	asch->schema = AdjectiveAmbiguity::schema_for_task(adj, K, task);
+	return 0;
 }
 
 @ Access:
diff --git a/inform7/assertions-module/Chapter 8/The Creation Predicates.w b/inform7/assertions-module/Chapter 8/The Creation Predicates.w
index 959750f1f..756243191 100644
--- a/inform7/assertions-module/Chapter 8/The Creation Predicates.w	
+++ b/inform7/assertions-module/Chapter 8/The Creation Predicates.w	
@@ -92,16 +92,18 @@ This relation is hard-wired in, and it is made in a slightly special way
 since (alone among binary predicates) it has no distinct reversal.
 
 =
-void CreationPredicates::stock_is_a_var(up_family *self, int n) {
+int CreationPredicates::stock_is_a_var(up_family *self, int n) {
 	if (n == 1) {
 		is_a_var_up = UnaryPredicates::new(is_a_var_up_family);
 	}
+	return 0;
 }
 
-void CreationPredicates::stock_is_a_const(up_family *self, int n) {
+int CreationPredicates::stock_is_a_const(up_family *self, int n) {
 	if (n == 1) {
 		is_a_const_up = UnaryPredicates::new(is_a_const_up_family);
 	}
+	return 0;
 }
 
 pcalc_prop *CreationPredicates::is_a_var_up(pcalc_term t) {
@@ -160,7 +162,7 @@ to |true|.
 
 =
 #ifdef CORE_MODULE
-void CreationPredicates::schema_calling(up_family *self, int task, unary_predicate *up,
+int CreationPredicates::schema_calling(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	switch(task) {
 		case TEST_ATOM_TASK:
@@ -171,25 +173,30 @@ void CreationPredicates::schema_calling(up_family *self, int task, unary_predica
 			asch->schema = NULL;
 			break;
 	}
+	return 0;
 }
 #endif
 
-void CreationPredicates::log_calling(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int CreationPredicates::log_calling(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("called='%W'", up->calling_name);
 	if (up->assert_kind) WRITE(":%u", up->assert_kind);
+	return 0;
 }
 
-void CreationPredicates::log_is_a_var(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int CreationPredicates::log_is_a_var(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("is-a-var");
+	return 0;
 }
 
-void CreationPredicates::log_is_a_const(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int CreationPredicates::log_is_a_const(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("is-a-const");
+	return 0;
 }
 
-void CreationPredicates::log_is_a_kind(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int CreationPredicates::log_is_a_kind(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("is-a-kind");
 	if (up->assert_kind) WRITE("=%u", up->assert_kind);
+	return 0;
 }
 
 int CreationPredicates::contains_callings(pcalc_prop *prop) {
diff --git a/inform7/assertions-module/Chapter 8/The Universal Relation.w b/inform7/assertions-module/Chapter 8/The Universal Relation.w
index f29472b2f..97e8ff15b 100644
--- a/inform7/assertions-module/Chapter 8/The Universal Relation.w	
+++ b/inform7/assertions-module/Chapter 8/The Universal Relation.w	
@@ -29,7 +29,7 @@ void Relations::Universal::start(void) {
 There are just two relations of this kind, and both are hard-wired in.
 
 =
-void Relations::Universal::stock(bp_family *self, int n) {
+int Relations::Universal::stock(bp_family *self, int n) {
 	if (n == 1) {
 		R_universal =
 			BinaryPredicates::make_pair(universal_bp_family,
@@ -42,6 +42,7 @@ void Relations::Universal::stock(bp_family *self, int n) {
 				I"means", NULL, NULL, NULL,
 				PreformUtilities::wording(<relation-names>, MEANING_RELATION_NAME));
 	}
+	return 0;
 }
 
 @h Typechecking.
diff --git a/inform7/if-module/Chapter 3/Backdrops.w b/inform7/if-module/Chapter 3/Backdrops.w
index f8f390dce..c41648119 100644
--- a/inform7/if-module/Chapter 3/Backdrops.w	
+++ b/inform7/if-module/Chapter 3/Backdrops.w	
@@ -248,9 +248,10 @@ inference *Backdrops::new_found_in_inference(inference_subject *loc, int certitu
 		STORE_POINTER_found_in_inference_data(data), certitude);
 }
 
-void Backdrops::log(inference_family *f, inference *inf) {
+int Backdrops::log(inference_family *f, inference *inf) {
 	found_in_inference_data *data = RETRIEVE_POINTER_found_in_inference_data(inf->data);
 	if (data->location) LOG(" in:$j", data->location);
+	return 0;
 }
 
 int Backdrops::cmp(inference_family *f, inference *i1, inference *i2) {
diff --git a/inform7/if-module/Chapter 3/Everywhere, Nowhere and Here.w b/inform7/if-module/Chapter 3/Everywhere, Nowhere and Here.w
index a5ea4f139..010e43950 100644
--- a/inform7/if-module/Chapter 3/Everywhere, Nowhere and Here.w	
+++ b/inform7/if-module/Chapter 3/Everywhere, Nowhere and Here.w	
@@ -57,14 +57,17 @@ pcalc_prop *WherePredicates::here_up(pcalc_term t) {
 @ These are automatically created, not defined in source text:
 
 =
-void WherePredicates::stock_everywhere(up_family *self, int n) {
+int WherePredicates::stock_everywhere(up_family *self, int n) {
 	if (n == 1) everywhere_up = UnaryPredicates::new(everywhere_up_family);
+	return 0;
 }
-void WherePredicates::stock_nowhere(up_family *self, int n) {
+int WherePredicates::stock_nowhere(up_family *self, int n) {
 	if (n == 1) nowhere_up = UnaryPredicates::new(nowhere_up_family);
+	return 0;
 }
-void WherePredicates::stock_here(up_family *self, int n) {
+int WherePredicates::stock_here(up_family *self, int n) {
 	if (n == 1) here_up = UnaryPredicates::new(here_up_family);
+	return 0;
 }
 
 @ Typechecking is a matter of verifying that these apply only to objects,
@@ -130,14 +133,14 @@ int WherePredicates::typecheck_here(up_family *self, unary_predicate *up,
 and for that we have to ask the Backdrops plugin to handle it.
 
 =
-void WherePredicates::assert_everywhere(up_family *self, unary_predicate *up,
+int WherePredicates::assert_everywhere(up_family *self, unary_predicate *up,
 	int now_negated, pcalc_prop *prop) {
 	if (now_negated) {
 		StandardProblems::sentence_problem(Task::syntax_tree(),
 			_p_(PM_CantAssertNegatedEverywhere),
 			"that seems to say that something isn't everywhere",
 			"which is too vague. You must say where it is.");
-		return;
+		return 0;
 	}
 	inference_subject *subj = Assert::subject_of_term(prop->terms[0]);
 	instance *ox = InstanceSubjects::to_object_instance(subj);
@@ -149,7 +152,7 @@ is out of play at the start of the narrative, but will appear later. So it
 is meaningful to assert that something is nowhere.
 
 =
-void WherePredicates::assert_nowhere(up_family *self, unary_predicate *up,
+int WherePredicates::assert_nowhere(up_family *self, unary_predicate *up,
 	int now_negated, pcalc_prop *prop) {
 	inference_subject *subj = Assert::subject_of_term(prop->terms[0]);
 	instance *ox = InstanceSubjects::to_object_instance(subj);
@@ -157,13 +160,13 @@ void WherePredicates::assert_nowhere(up_family *self, unary_predicate *up,
 		StandardProblems::sentence_problem(Task::syntax_tree(), _p_(BelievedImpossible),
 			"that seems to say that something isn't nowhere",
 			"which is too vague. You must say where it is.");
-		return;
+		return 0;
 	}
 	if (ox == NULL) {
 		StandardProblems::sentence_problem(Task::syntax_tree(), _p_(BelievedImpossible),
 			"that seems to say that something generic is 'nowhere'",
 			"which suggests it could some day have a physical location.");
-		return;
+		return 0;
 	}
 	Spatial::infer_presence_nowhere(ox);
 }
@@ -174,7 +177,7 @@ might not yet know what is and is not a room. So we record a special inference
 and put the issue aside for now.
 
 =
-void WherePredicates::assert_here(up_family *self, unary_predicate *up,
+int WherePredicates::assert_here(up_family *self, unary_predicate *up,
 	int now_negated, pcalc_prop *prop) {
 	inference_subject *subj = Assert::subject_of_term(prop->terms[0]);
 	instance *ox = InstanceSubjects::to_object_instance(subj);
@@ -182,27 +185,30 @@ void WherePredicates::assert_here(up_family *self, unary_predicate *up,
 		StandardProblems::sentence_problem(Task::syntax_tree(), _p_(BelievedImpossible),
 			"that seems to say that something isn't here",
 			"which is too vague. You must say where it is.");
-		return;
+		return 0;
 	}
 	if (ox == NULL) {
 		StandardProblems::sentence_problem(Task::syntax_tree(), _p_(PM_NonInstanceHere),
 			"that seems to say that something generic is 'here'",
 			"which would give it a physical location. (It would be like saying "
 			"'A number is here' - well, numbers are everywhere and nowhere.)");
-		return;
+		return 0;
 	}
 	Spatial::infer_presence_here(ox);
 }
 
 @ =
-void WherePredicates::log_everywhere(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int WherePredicates::log_everywhere(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("everywhere");
+	return 0;
 }
 
-void WherePredicates::log_nowhere(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int WherePredicates::log_nowhere(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("nowhere");
+	return 0;
 }
 
-void WherePredicates::log_here(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int WherePredicates::log_here(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	WRITE("here");
+	return 0;
 }
diff --git a/inform7/if-module/Chapter 3/Map Connection Relations.w b/inform7/if-module/Chapter 3/Map Connection Relations.w
index 3cae0feaf..98d332fcb 100644
--- a/inform7/if-module/Chapter 3/Map Connection Relations.w	
+++ b/inform7/if-module/Chapter 3/Map Connection Relations.w	
@@ -194,9 +194,10 @@ int MapRelations::assert(bp_family *self, binary_predicate *bp,
 @h Indexing.
 
 =
-void MapRelations::describe_for_index(bp_family *self, OUTPUT_STREAM,
+int MapRelations::describe_for_index(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	WRITE("map");
+	return 0;
 }
 
 @h The correspondence with directions.
diff --git a/inform7/if-module/Chapter 3/Spatial Inferences.w b/inform7/if-module/Chapter 3/Spatial Inferences.w
index b54739d3b..6a922323c 100644
--- a/inform7/if-module/Chapter 3/Spatial Inferences.w	
+++ b/inform7/if-module/Chapter 3/Spatial Inferences.w	
@@ -82,9 +82,10 @@ void SpatialInferences::infer_parentage(inference_subject *inner, int certitude,
 	Inferences::join_inference(i, inner);
 }
 
-void SpatialInferences::log_parentage_inf(inference_family *f, inference *inf) {
+int SpatialInferences::log_parentage_inf(inference_family *f, inference *inf) {
 	parentage_inference_data *data = RETRIEVE_POINTER_parentage_inference_data(inf->data);
 	if (data->parent) LOG(" parent:$j", data->parent);
+	return 0;
 }
 
 int SpatialInferences::cmp_parentage_inf(inference_family *f, inference *i1, inference *i2) {
@@ -142,10 +143,11 @@ void SpatialInferences::infer_parentage_here(inference_subject *inner, int certi
 	Inferences::join_inference(i, inner);
 }
 
-void SpatialInferences::log_parentage_here(inference_family *f, inference *inf) {
+int SpatialInferences::log_parentage_here(inference_family *f, inference *inf) {
 	parentage_here_inference_data *data =
 		RETRIEVE_POINTER_parentage_here_inference_data(inf->data);
 	if (data->parent) LOG(" parent:$j", data->parent);
+	return 0;
 }
 
 int SpatialInferences::cmp_parentage_here(inference_family *f, inference *i1,
@@ -190,9 +192,10 @@ typedef struct part_of_inference_data {
 } part_of_inference_data;
 
 
-void SpatialInferences::log_part_of(inference_family *f, inference *inf) {
+int SpatialInferences::log_part_of(inference_family *f, inference *inf) {
 	part_of_inference_data *data = RETRIEVE_POINTER_part_of_inference_data(inf->data);
 	if (data->parent) LOG(" part-of:$j", data->parent);
+	return 0;
 }
 
 int SpatialInferences::cmp_part_of(inference_family *f, inference *i1, inference *i2) {
diff --git a/inform7/if-module/Chapter 3/Spatial Relations.w b/inform7/if-module/Chapter 3/Spatial Relations.w
index e2b7ab2cb..824fbd6fc 100644
--- a/inform7/if-module/Chapter 3/Spatial Relations.w	
+++ b/inform7/if-module/Chapter 3/Spatial Relations.w	
@@ -31,13 +31,14 @@ void SpatialRelations::start(void) {
 		SpatialRelations::describe_for_index);
 }
 
-void SpatialRelations::stock(bp_family *self, int n) {
+int SpatialRelations::stock(bp_family *self, int n) {
 	if (n == 1) {
 		@<Make built-in spatial relationships@>;
 		@<Make built-in indirect spatial relationships@>;
 		MapRelations::create_relations();
 		Regions::create_relations();
 	}
+	return 0;
 }
 
 @ Containment, support, incorporation, carrying, holding, wearing and
@@ -270,7 +271,8 @@ special to make it work, so this doesn't seem worth the trouble.)
 @h Cursory description.
 
 =
-void SpatialRelations::describe_for_index(bp_family *self, OUTPUT_STREAM,
+int SpatialRelations::describe_for_index(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	WRITE("spatial");
+	return 0;
 }
diff --git a/inform7/if-module/Chapter 3/The Map.w b/inform7/if-module/Chapter 3/The Map.w
index 9315f26ec..6a3e8accd 100644
--- a/inform7/if-module/Chapter 3/The Map.w	
+++ b/inform7/if-module/Chapter 3/The Map.w	
@@ -117,9 +117,10 @@ void Map::infer_direction(inference_subject *infs_from, inference_subject *infs_
 	Inferences::join_inference(i, infs_from);
 }
 
-void Map::log_direction_inf(inference_family *f, inference *inf) {
+int Map::log_direction_inf(inference_family *f, inference *inf) {
 	direction_inference_data *data = RETRIEVE_POINTER_direction_inference_data(inf->data);
 	LOG("-to:$j -dir:$j", data->to, data->dir);
+	return 0;
 }
 
 @ Inferences about different directions from a location are different topics;
diff --git a/inform7/knowledge-module/Chapter 3/Comparative Relations.w b/inform7/knowledge-module/Chapter 3/Comparative Relations.w
index b90db2531..8f687c916 100644
--- a/inform7/knowledge-module/Chapter 3/Comparative Relations.w	
+++ b/inform7/knowledge-module/Chapter 3/Comparative Relations.w	
@@ -32,8 +32,9 @@ This is where our comparative relations come from, but the work is done in
 //Measurement Adjectives//, not here.
 
 =
-void ComparativeRelations::stock(bp_family *self, int n) {
+int ComparativeRelations::stock(bp_family *self, int n) {
 	if (n == 2) Measurements::create_comparatives();
+	return 0;
 }
 
 @ However, whenever a comparative is made, an instance of the following is
diff --git a/inform7/knowledge-module/Chapter 3/Either-Or Property Adjectives.w b/inform7/knowledge-module/Chapter 3/Either-Or Property Adjectives.w
index 50a0a8fbf..76c7503ee 100644
--- a/inform7/knowledge-module/Chapter 3/Either-Or Property Adjectives.w	
+++ b/inform7/knowledge-module/Chapter 3/Either-Or Property Adjectives.w	
@@ -58,11 +58,12 @@ int EitherOrPropertyAdjectives::assert(adjective_meaning_family *f,
 	return TRUE;
 }
 
-void EitherOrPropertyAdjectives::prepare_schemas(adjective_meaning_family *family,
+int EitherOrPropertyAdjectives::prepare_schemas(adjective_meaning_family *family,
 	adjective_meaning *am, int T) {
 	property *prn = RETRIEVE_POINTER_property(am->family_specific_data);
 	if (am->schemas_prepared == FALSE)
 		RTProperties::write_either_or_schemas(am, prn, T);
+	return 0;
 }
 
 int EitherOrPropertyAdjectives::index(adjective_meaning_family *f, text_stream *OUT,
diff --git a/inform7/knowledge-module/Chapter 3/Measurement Adjectives.w b/inform7/knowledge-module/Chapter 3/Measurement Adjectives.w
index c22d17f84..e0f2515e2 100644
--- a/inform7/knowledge-module/Chapter 3/Measurement Adjectives.w	
+++ b/inform7/knowledge-module/Chapter 3/Measurement Adjectives.w	
@@ -187,10 +187,11 @@ int MeasurementAdjectives::assert(adjective_meaning_family *f,
 @h Schemas.
 
 =
-void MeasurementAdjectives::prepare_schemas(adjective_meaning_family *family,
+int MeasurementAdjectives::prepare_schemas(adjective_meaning_family *family,
 	adjective_meaning *am, int T) {
 	measurement_definition *mdef =
 		RETRIEVE_POINTER_measurement_definition(am->family_specific_data);
 	if ((mdef->prop) && (mdef->region_threshold_evaluated))
 		RTAdjectives::make_mdef_test_schema(mdef, T);
+	return 0;
 }
diff --git a/inform7/knowledge-module/Chapter 3/Same Property Relation.w b/inform7/knowledge-module/Chapter 3/Same Property Relation.w
index 50664d6fb..d4ec004f7 100644
--- a/inform7/knowledge-module/Chapter 3/Same Property Relation.w	
+++ b/inform7/knowledge-module/Chapter 3/Same Property Relation.w	
@@ -30,7 +30,7 @@ than" -- see //Comparative Relations// -- because it is unclear just how much
 taller than Big Bird we would have to make C. J.
 
 =
-void SameAsRelations::stock(bp_family *self, int n) {
+int SameAsRelations::stock(bp_family *self, int n) {
 	if (n == 2) {
 		property *prn;
 		LOOP_OVER(prn, property) {
@@ -56,6 +56,7 @@ void SameAsRelations::stock(bp_family *self, int n) {
 			}
 		}
 	}
+	return 0;
 }
 
 @ The family-specific data in this family is just a pointer to the property:
diff --git a/inform7/knowledge-module/Chapter 3/Setting Property Relation.w b/inform7/knowledge-module/Chapter 3/Setting Property Relation.w
index 9419da188..5ec7ea692 100644
--- a/inform7/knowledge-module/Chapter 3/Setting Property Relation.w	
+++ b/inform7/knowledge-module/Chapter 3/Setting Property Relation.w	
@@ -30,7 +30,7 @@ The case of |n| being 2 is when all properties have been created, making it
 the perfect opportunity to go over all of the property-setting BPs:
 
 =
-void SettingPropertyRelations::stock(bp_family *self, int n) {
+int SettingPropertyRelations::stock(bp_family *self, int n) {
 	if (n == 2) {
 		binary_predicate *bp;
 		LOOP_OVER(bp, binary_predicate)
@@ -41,6 +41,7 @@ void SettingPropertyRelations::stock(bp_family *self, int n) {
 					SettingPropertyRelations::fix_property_bp(bp);
 			}
 	}
+	return 0;
 }
 
 @h Subsequent creations.
diff --git a/inform7/knowledge-module/Chapter 3/The Provision Relation.w b/inform7/knowledge-module/Chapter 3/The Provision Relation.w
index f52664f13..42658db84 100644
--- a/inform7/knowledge-module/Chapter 3/The Provision Relation.w	
+++ b/inform7/knowledge-module/Chapter 3/The Provision Relation.w	
@@ -31,7 +31,7 @@ There's just one relation of this kind, and it's hard-wired in. The terms are
 given a very broad range of what they can accept.
 
 =
-void ProvisionRelation::stock(bp_family *self, int n) {
+int ProvisionRelation::stock(bp_family *self, int n) {
 	if (n == 1) {
 		R_provision =
 			BinaryPredicates::make_pair(provision_bp_family,
@@ -40,6 +40,7 @@ void ProvisionRelation::stock(bp_family *self, int n) {
 				PreformUtilities::wording(<relation-names>, PROVISION_RELATION_NAME));
 		BinaryPredicates::set_index_details(R_provision, "value", "property");
 	}
+	return 0;
 }
 
 @h Typechecking.
@@ -94,7 +95,8 @@ int ProvisionRelation::schema(bp_family *self, int task, binary_predicate *bp,
 Nothing special is needed here.
 
 =
-void ProvisionRelation::describe_for_index(bp_family *self, OUTPUT_STREAM,
+int ProvisionRelation::describe_for_index(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	WRITE("provision");
+	return 0;
 }
diff --git a/inform7/knowledge-module/Chapter 4/Instance Subjects.w b/inform7/knowledge-module/Chapter 4/Instance Subjects.w
index 43cce1021..6c41f1835 100644
--- a/inform7/knowledge-module/Chapter 4/Instance Subjects.w	
+++ b/inform7/knowledge-module/Chapter 4/Instance Subjects.w	
@@ -49,18 +49,21 @@ instance *InstanceSubjects::to_object_instance(inference_subject *infs) {
 	return NULL;
 }
 
-void InstanceSubjects::get_name(inference_subject_family *family,
+int InstanceSubjects::get_name(inference_subject_family *family,
 	inference_subject *from, wording *W) {
 	instance *I = InstanceSubjects::to_instance(from);
 	*W = Instances::get_name(I, FALSE);
+	return 0;
 }
 
-void InstanceSubjects::new_permission_granted(inference_subject_family *f,
+int InstanceSubjects::new_permission_granted(inference_subject_family *f,
 	inference_subject *from, property_permission *pp) {
 	RTPropertyPermissions::new_storage(pp);
+	return 0;
 }
 
-void InstanceSubjects::make_adj_const_domain(inference_subject_family *family,
+int InstanceSubjects::make_adj_const_domain(inference_subject_family *family,
 	inference_subject *S, instance *I, property *P) {
 	InstanceAdjectives::make_adjectival(I, P, NULL, InstanceSubjects::to_instance(S));
+	return 0;
 }
diff --git a/inform7/knowledge-module/Chapter 4/Kind Subjects.w b/inform7/knowledge-module/Chapter 4/Kind Subjects.w
index 014f54d59..c9f5829bc 100644
--- a/inform7/knowledge-module/Chapter 4/Kind Subjects.w	
+++ b/inform7/knowledge-module/Chapter 4/Kind Subjects.w	
@@ -110,9 +110,10 @@ int KindSubjects::has_properties(kind *K) {
 concerning our kind.
 
 =
-void KindSubjects::new_permission_granted(inference_subject_family *f,
+int KindSubjects::new_permission_granted(inference_subject_family *f,
 	inference_subject *from, property_permission *pp) {
 	RTPropertyPermissions::new_storage(pp);
+	return 0;
 }
 
 @ When a property value comes along which might have an adjectival use -- say,
@@ -120,17 +121,19 @@ void KindSubjects::new_permission_granted(inference_subject_family *f,
 We simply pass the request along to the appropriate code.
 
 =
-void KindSubjects::make_adj_const_domain(inference_subject_family *family,
+int KindSubjects::make_adj_const_domain(inference_subject_family *family,
 	inference_subject *infs, instance *I, property *prn) {
 	kind *K = KindSubjects::to_kind(infs);
 	InstanceAdjectives::make_adjectival(I, prn, K, NULL);
+	return 0;
 }
 
 @ =
-void KindSubjects::get_name_text(inference_subject_family *family,
+int KindSubjects::get_name_text(inference_subject_family *family,
 	inference_subject *from, wording *W) {
 	kind *K = KindSubjects::to_kind(from);
 	*W = Kinds::Behaviour::get_name(K, FALSE);
+	return 0;
 }
 
 @ Inferences about kinds are considered only likely to be true of something
diff --git a/inform7/knowledge-module/Chapter 4/Relation Subjects.w b/inform7/knowledge-module/Chapter 4/Relation Subjects.w
index d701ae761..5c91b07a2 100644
--- a/inform7/knowledge-module/Chapter 4/Relation Subjects.w	
+++ b/inform7/knowledge-module/Chapter 4/Relation Subjects.w	
@@ -39,20 +39,21 @@ int RelationSubjects::certainty(inference_subject_family *f, inference_subject *
 	return CERTAIN_CE;	
 }
 
-void RelationSubjects::get_name(inference_subject_family *family,
+int RelationSubjects::get_name(inference_subject_family *family,
 	inference_subject *from, wording *W) {
 	*W = EMPTY_WORDING; /* nameless */
+	return 0;
 }
 
 @ At model completion time, we need to do some work on equivalence relations:
 
 =
-void RelationSubjects::complete_model(inference_subject_family *family,
+int RelationSubjects::complete_model(inference_subject_family *family,
 	inference_subject *infs) {
 	int domain_size = NUMBER_CREATED(inference_subject);
 	binary_predicate *bp = RelationSubjects::to_bp(infs);
 
-	if (ExplicitRelations::stored_dynamically(bp)) return; /* handled at run-time */
+	if (ExplicitRelations::stored_dynamically(bp)) return 0; /* handled at run-time */
 
 	if ((ExplicitRelations::get_form_of_relation(bp) == Relation_Equiv) &&
 		(bp->right_way_round)) {
@@ -76,7 +77,7 @@ But in case the implementation is ever changed, it seems prudent to leave this
 checking in.
 
 =
-void RelationSubjects::check_model(inference_subject_family *family,
+int RelationSubjects::check_model(inference_subject_family *family,
 	inference_subject *infs) {
 	binary_predicate *bp = RelationSubjects::to_bp(infs);
 	int f = ExplicitRelations::get_form_of_relation(bp);
@@ -84,6 +85,7 @@ void RelationSubjects::check_model(inference_subject_family *family,
 		RelationSubjects::check_OtoO_relation(bp);
 	if ((bp->right_way_round) && ((f == Relation_OtoV) || (f == Relation_VtoO)))
 		RelationSubjects::check_OtoV_relation(bp);
+	return 0;
 }
 
 void RelationSubjects::check_OtoO_relation(binary_predicate *bp) {
diff --git a/inform7/knowledge-module/Chapter 4/Variable Subjects.w b/inform7/knowledge-module/Chapter 4/Variable Subjects.w
index e310d2bbd..38aa4a532 100644
--- a/inform7/knowledge-module/Chapter 4/Variable Subjects.w	
+++ b/inform7/knowledge-module/Chapter 4/Variable Subjects.w	
@@ -36,10 +36,11 @@ inference_subject *VariableSubjects::new(nonlocal_variable *nlv) {
 		VariableSubjects::family(), STORE_POINTER_nonlocal_variable(nlv), NULL);
 }
 
-void VariableSubjects::get_name(inference_subject_family *family,
+int VariableSubjects::get_name(inference_subject_family *family,
 	inference_subject *from, wording *W) {
 	nonlocal_variable *nlv = VariableSubjects::to_variable(from);
 	*W = nlv->name;
+	return 0;
 }
 
 @ The initial value of a variable is stored as the value of the subject's
@@ -167,7 +168,7 @@ that the //assertions: Property Knowledge// code will throw problem
 messages if these have the wrong kind, so we don't need to.
 
 =
-void VariableSubjects::check_model(inference_subject_family *family,
+int VariableSubjects::check_model(inference_subject_family *family,
 	inference_subject *infs) {
 	nonlocal_variable *nlv = VariableSubjects::to_variable(infs);
 	if (nlv) {
@@ -178,6 +179,7 @@ void VariableSubjects::check_model(inference_subject_family *family,
 			VariableSubjects::typecheck_initial_value(nlv, init, TRUE);
 		}
 	}
+	return 0;
 }
 
 @ If a variable is said to be the same as, say, |my_var| defined in some kit
diff --git a/inform7/knowledge-module/Chapter 5/Property Inferences.w b/inform7/knowledge-module/Chapter 5/Property Inferences.w
index 81b5d7751..ecf39c62a 100644
--- a/inform7/knowledge-module/Chapter 5/Property Inferences.w	
+++ b/inform7/knowledge-module/Chapter 5/Property Inferences.w	
@@ -63,10 +63,11 @@ void PropertyInferences::draw_negated(inference_subject *subj, property *prn, pa
 @ So now for the methods:
 
 =
-void PropertyInferences::log_details(inference_family *f, inference *inf) {
+int PropertyInferences::log_details(inference_family *f, inference *inf) {
 	property_inference_data *data = RETRIEVE_POINTER_property_inference_data(inf->data);
 	LOG("(%W)", data->inferred_property->name);
 	if (data->inferred_property_value) LOG(":=$P", data->inferred_property_value);
+	return 0;
 }
 
 @ By convention, a pair of attached either/or properties which are negations of
diff --git a/inform7/knowledge-module/Chapter 5/Relation Inferences.w b/inform7/knowledge-module/Chapter 5/Relation Inferences.w
index 45d2e525a..9b4f142c8 100644
--- a/inform7/knowledge-module/Chapter 5/Relation Inferences.w	
+++ b/inform7/knowledge-module/Chapter 5/Relation Inferences.w	
@@ -62,12 +62,13 @@ void RelationInferences::draw_spec(binary_predicate *bp,
 @ And here are the method calls:
 
 =
-void RelationInferences::log_details(inference_family *f, inference *inf) {
+int RelationInferences::log_details(inference_family *f, inference *inf) {
 	relation_inference_data *data = RETRIEVE_POINTER_relation_inference_data(inf->data);
 	if (data->terms_as_subjects[0]) LOG("-1:$j", data->terms_as_subjects[0]);
 	if (data->terms_as_subjects[1]) LOG("-2:$j", data->terms_as_subjects[1]);
 	if (data->terms_as_values[0]) LOG("-s1:$P", data->terms_as_values[0]);
 	if (data->terms_as_values[1]) LOG("-s2:$P", data->terms_as_values[1]);
+	return 0;
 }
 
 @ Note that the topic of a relation inference depends on both terms. If
diff --git a/inform7/runtime-module/Chapter 2/Here, Nowhere and Everywhere.w b/inform7/runtime-module/Chapter 2/Here, Nowhere and Everywhere.w
index c7cd7735f..79c533a66 100644
--- a/inform7/runtime-module/Chapter 2/Here, Nowhere and Everywhere.w	
+++ b/inform7/runtime-module/Chapter 2/Here, Nowhere and Everywhere.w	
@@ -8,7 +8,7 @@ be compiled, so this code is never used. But the following would be correct if w
 ever change our minds about that.
 
 =
-void RTSpatial::schema_here(up_family *self, int task, unary_predicate *up,
+int RTSpatial::schema_here(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	switch(task) {
 		case TEST_ATOM_TASK:
@@ -21,12 +21,13 @@ void RTSpatial::schema_here(up_family *self, int task, unary_predicate *up,
 				"so it cannot be brought about or cancelled out with 'now'.");
 			asch->schema = NULL; break;
 	}
+	return 0;
 }
 
 @ Everywhere.
 
 =
-void RTSpatial::schema_everywhere(up_family *self, int task, unary_predicate *up,
+int RTSpatial::schema_everywhere(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	switch(task) {
 		case TEST_ATOM_TASK:
@@ -44,12 +45,13 @@ void RTSpatial::schema_everywhere(up_family *self, int task, unary_predicate *up
 				"because it's not exact enough about what needs to be done.");
 			asch->schema = NULL; break;
 	}
+	return 0;
 }
 
 @ Nowhere.
 
 =
-void RTSpatial::schema_nowhere(up_family *self, int task, unary_predicate *up,
+int RTSpatial::schema_nowhere(up_family *self, int task, unary_predicate *up,
 	annotated_i6_schema *asch, kind *K) {
 	switch(task) {
 		case TEST_ATOM_TASK:
@@ -63,4 +65,5 @@ void RTSpatial::schema_nowhere(up_family *self, int task, unary_predicate *up,
 				"MoveObject(*1, real_location, 1, false);");
 			break;
 	}
+	return 0;
 }
diff --git a/inter/building-module/Chapter 2/Inter Schemas.w b/inter/building-module/Chapter 2/Inter Schemas.w
index 6db7c1f6a..ac0d460d9 100644
--- a/inter/building-module/Chapter 2/Inter Schemas.w	
+++ b/inter/building-module/Chapter 2/Inter Schemas.w	
@@ -584,10 +584,10 @@ void InterSchemas::lint(inter_schema *sch) {
 text_stream *InterSchemas::lint_isn(inter_schema_node *isn, int depth) {
 	for (; isn; isn=isn->next_node) {
 		if (isn->isn_type == EXPRESSION_ISNT) {
-			int asm = FALSE;
+			int asm_flag = FALSE;
 			for (inter_schema_token *t = isn->expression_tokens; t; t=t->next) {
 				switch (t->ist_type) {
-					case OPCODE_ISTT:		if (t == isn->expression_tokens) asm = TRUE;
+					case OPCODE_ISTT:		if (t == isn->expression_tokens) asm_flag = TRUE;
 											else return I"contains loose assembler";
 											break;
 					case RAW_ISTT:			return I"contains raw node";
@@ -606,7 +606,7 @@ text_stream *InterSchemas::lint_isn(inter_schema_node *isn, int depth) {
 				if ((t->ist_type == INLINE_ISTT) && (t->inline_command == close_brace_ISINC))
 					return I"contains manual close-brace";
 				if ((t->ist_type == NUMBER_ISTT) && (t->next) &&
-					(t->next->ist_type == NUMBER_ISTT) && (asm == FALSE))
+					(t->next->ist_type == NUMBER_ISTT) && (asm_flag == FALSE))
 					return I"two consecutive numbers";
 				if (isn != t->owner)
 					return I"ownership linkage broken";
diff --git a/inter/building-module/Chapter 2/Ramification.w b/inter/building-module/Chapter 2/Ramification.w
index bae183171..acc782475 100644
--- a/inter/building-module/Chapter 2/Ramification.w	
+++ b/inter/building-module/Chapter 2/Ramification.w	
@@ -1878,10 +1878,10 @@ to cause trouble later. If no error is thrown, the schema is unchanged.
 int Ramification::sanity_check(inter_schema_node *par, inter_schema_node *isn) {
 	for (; isn; isn=isn->next_node) {
 		if (isn->isn_type == EXPRESSION_ISNT) {
-			int asm = FALSE;
+			int asm_flag = FALSE;
 			for (inter_schema_token *t = isn->expression_tokens; t; t=t->next) {
 				switch (t->ist_type) {
-					case OPCODE_ISTT:		asm = TRUE; break;
+					case OPCODE_ISTT:		asm_flag = TRUE; break;
 					case RAW_ISTT:			InterSchemas::throw_error(isn, I"malformed expression"); break;
 					case OPEN_BRACE_ISTT:	InterSchemas::throw_error(isn, I"unexpected '{'"); break;
 					case CLOSE_BRACE_ISTT:	InterSchemas::throw_error(isn, I"unexpected '}'"); break;
@@ -1900,7 +1900,7 @@ int Ramification::sanity_check(inter_schema_node *par, inter_schema_node *isn) {
 					case OPERATOR_ISTT:		InterSchemas::throw_error(isn, I"unexpected operator"); break;
 				}
 				if ((t->ist_type == NUMBER_ISTT) && (t->next) &&
-					(t->next->ist_type == NUMBER_ISTT) && (asm == FALSE))
+					(t->next->ist_type == NUMBER_ISTT) && (asm_flag == FALSE))
 					InterSchemas::throw_error(isn, I"two consecutive numbers");
 			}
 			if (isn->child_node) InterSchemas::throw_error(isn, I"malformed expression");
diff --git a/inter/bytecode-module/Chapter 4/The Comment Construct.w b/inter/bytecode-module/Chapter 4/The Comment Construct.w
index e70fc89f6..3680e3b7a 100644
--- a/inter/bytecode-module/Chapter 4/The Comment Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Comment Construct.w	
@@ -40,34 +40,37 @@ inter_error_message *CommentInstruction::new(inter_bookmark *IBM, text_stream *c
 	return NULL;
 }
 
-void CommentInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int CommentInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[TEXT_COMMENT_IFLD] = grid[P->W.instruction[TEXT_COMMENT_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void CommentInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int CommentInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::text_field(owner, P, TEXT_COMMENT_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void CommentInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int CommentInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *commentary = ilp->mr.exp[0];
 	*E = CommentInstruction::new(IBM, commentary, eloc, (inter_ti) ilp->indent_level);
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 The empty comment is printed back as a blank line, rather than a lone |#|.
 
 =
-void CommentInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int CommentInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	text_stream *S = Inode::ID_to_text(P, P->W.instruction[TEXT_COMMENT_IFLD]);
 	if (Str::len(S) > 0) WRITE("#%S", S);
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 4/The Constant Construct.w b/inter/bytecode-module/Chapter 4/The Constant Construct.w
index 4bd917e04..f72d28257 100644
--- a/inter/bytecode-module/Chapter 4/The Constant Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Constant Construct.w	
@@ -122,43 +122,44 @@ inter_error_message *ConstantInstruction::new_list(inter_bookmark *IBM, inter_sy
 	return NULL;
 }
 
-void ConstantInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int ConstantInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	for (int i=DATA_CONST_IFLD; i<P->W.extent; i=i+2)
 		InterValuePairs::set(P, i,
 			InterValuePairs::transpose(InterValuePairs::get(P, i), grid, grid_extent, E));
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void ConstantInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int ConstantInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	if ((P->W.extent % 2) != 1) {
-		*E = Inode::error(P, I"extent not an odd number", NULL); return;
+		*E = Inode::error(P, I"extent not an odd number", NULL); return 0;
 	}
 	int data_fields = (P->W.extent - DATA_CONST_IFLD)/2;
 	inter_ti format = P->W.instruction[FORMAT_CONST_IFLD];
 	if ((format == CONST_LIST_FORMAT_NONE) && (data_fields != 1)) {
-		*E = Inode::error(P, I"extent wrong", NULL); return;
+		*E = Inode::error(P, I"extent wrong", NULL); return 0;
 	}
 
 	*E = VerifyingInter::TID_field(owner, P, TYPE_CONST_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	
 	if (format > CONST_LIST_FORMAT_STRUCT) {
-		*E = Inode::error(P, I"no such constant format", NULL); return;
+		*E = Inode::error(P, I"no such constant format", NULL); return 0;
 	}
 
 	inter_type type = InterTypes::from_TID_in_field(P, TYPE_CONST_IFLD);
 	if (format == CONST_LIST_FORMAT_STRUCT) {
 		if (data_fields != InterTypes::type_arity(type)) {
-			*E = Inode::error(P, I"extent not same as struct length", NULL); return;
+			*E = Inode::error(P, I"extent not same as struct length", NULL); return 0;
 		}
 		for (int i=DATA_CONST_IFLD, counter = 0; i<P->W.extent; i=i+2) {
 			inter_type field_type = InterTypes::type_operand(type, counter++);
 			*E = VerifyingInter::data_pair_fields(owner, P, i, field_type);
-			if (*E) return;
+			if (*E) return 0;
 		}
 	} else {
 		inter_type verify_type = type;
@@ -166,7 +167,7 @@ void ConstantInstruction::verify(inter_construct *IC, inter_tree_node *P,
 			verify_type = InterTypes::type_operand(type, 0);
 		for (int i=DATA_CONST_IFLD; i<P->W.extent; i=i+2) {
 			*E = VerifyingInter::data_pair_fields(owner, P, i, verify_type);
-			if (*E) return;
+			if (*E) return 0;
 		}
 	}
 }
@@ -174,14 +175,14 @@ void ConstantInstruction::verify(inter_construct *IC, inter_tree_node *P,
 @h Creating from textual Inter syntax.
 
 =
-void ConstantInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int ConstantInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *name_text = ilp->mr.exp[0], *value_text = ilp->mr.exp[1];
 
 	inter_type con_type;
 	inter_symbol *con_name = NULL;
 	@<Parse the type and name@>;
-	if (*E) return;
+	if (*E) return 0;
 
 	SymbolAnnotation::copy_set_to_symbol(&(ilp->set), con_name);
 
@@ -293,7 +294,7 @@ void ConstantInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void ConstantInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int ConstantInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *con_name = InterSymbolsTable::symbol_from_ID_at_node(P, DEFN_CONST_IFLD);
 	WRITE("constant ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_CONST_IFLD);
@@ -329,6 +330,7 @@ void ConstantInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_n
 		if (ConstantInstruction::is_a_byte_format(fmt)) WRITE(" bytes");
 		else WRITE(" words");
 	}
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Insert Construct.w b/inter/bytecode-module/Chapter 4/The Insert Construct.w
index fc2ba493f..b5ef74f88 100644
--- a/inter/bytecode-module/Chapter 4/The Insert Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Insert Construct.w	
@@ -44,26 +44,27 @@ inter_error_message *InsertInstruction::new(inter_bookmark *IBM,
 	return NULL;
 }
 
-void InsertInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int InsertInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[TEXT_INSERT_IFLD] = grid[P->W.instruction[TEXT_INSERT_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void InsertInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int InsertInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::text_field(owner, P, TEXT_INSERT_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::text_field(owner, P, REPLACING_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void InsertInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int InsertInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *rq = ilp->mr.exp[0];
 	text_stream *repq = ilp->mr.exp[1];
@@ -76,18 +77,20 @@ void InsertInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_lin
 		*E = InsertInstruction::new(IBM, raw, replacing, (inter_ti) ilp->indent_level, eloc);
 	DISCARD_TEXT(raw)
 	DISCARD_TEXT(replacing)
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void InsertInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int InsertInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	text_stream *insertion = InsertInstruction::insertion(P);
 	text_stream *replacing = InsertInstruction::replacing(P);
 	WRITE("insert ");
 	TextualInter::write_text(OUT, insertion);
 	WRITE(" ");
 	TextualInter::write_text(OUT, replacing);
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 4/The Instance Construct.w b/inter/bytecode-module/Chapter 4/The Instance Construct.w
index 963dfeca3..bddb76105 100644
--- a/inter/bytecode-module/Chapter 4/The Instance Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Instance Construct.w	
@@ -49,12 +49,13 @@ inter_error_message *InstanceInstruction::new(inter_bookmark *IBM, inter_symbol
 	return NULL;
 }
 
-void InstanceInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int InstanceInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[PROP_LIST_INST_IFLD] = grid[P->W.instruction[PROP_LIST_INST_IFLD]];
 	P->W.instruction[PERM_LIST_INST_IFLD] = grid[P->W.instruction[PERM_LIST_INST_IFLD]];
 	InterValuePairs::set(P, VAL1_INST_IFLD,
 		InterValuePairs::transpose(InterValuePairs::get(P, VAL1_INST_IFLD), grid, grid_extent, E));
+	return 0;
 }
 
 @ Verification does more than making sanity checks: it also calculates and sets
@@ -62,17 +63,17 @@ the enumerated value of the instance (if it is not already set), and notifies
 the typename_s that it has a new instance.
 
 =
-void InstanceInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int InstanceInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	*E = VerifyingInter::SID_field(owner, P, TYPE_INST_IFLD, TYPENAME_IST);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::node_list_field(owner, P, PROP_LIST_INST_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::node_list_field(owner, P, PERM_LIST_INST_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
-void InstanceInstruction::xref(inter_construct *IC, inter_tree_node *P,
+int InstanceInstruction::xref(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	inter_symbol *typename_s = InterSymbolsTable::symbol_from_ID_at_node(P, TYPE_INST_IFLD);
 	inter_type inst_type = InterTypes::from_type_name(typename_s);
@@ -81,11 +82,11 @@ void InstanceInstruction::xref(inter_construct *IC, inter_tree_node *P,
 			InterValuePairs::set(P, VAL1_INST_IFLD,
 				InterValuePairs::number(TypenameInstruction::next_enumerated_value(typename_s)));
 	} else {
-		*E = Inode::error(P, I"not a kind which has instances", NULL); return;
+		*E = Inode::error(P, I"not a kind which has instances", NULL); return 0;
 	}
 	inter_package *owner = Inode::get_package(P);
 	*E = VerifyingInter::data_pair_fields(owner, P, VAL1_INST_IFLD, inst_type);
-	if (*E) return;
+	if (*E) return 0;
 
 	inter_symbol *instance_s = InstanceInstruction::instance(P);
 	TypenameInstruction::new_instance(typename_s, instance_s);
@@ -94,7 +95,7 @@ void InstanceInstruction::xref(inter_construct *IC, inter_tree_node *P,
 @h Creating from textual Inter syntax.
 
 =
-void InstanceInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int InstanceInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = ilp->mr.exp[0];
 	text_stream *instance_text = ilp->mr.exp[1];
@@ -108,6 +109,7 @@ void InstanceInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_l
 
 	*E = InstanceInstruction::new(IBM, instance_s, InterTypes::type_name(inst_type),
 		val, (inter_ti) ilp->indent_level, eloc);
+	return 0;
 }
 
 @<Find the enumerated type this will belong to@> =
@@ -115,7 +117,7 @@ void InstanceInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_l
 	if (Regexp::match(&mr, type_text, L"%((%c+)%)"))
 		inst_type = InterTypes::parse_simple(InterBookmark::scope(IBM), eloc, mr.exp[0], E);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 @<Find the instance name and enumerated value, if given@> =
 	text_stream *value_text = NULL;
@@ -129,17 +131,18 @@ void InstanceInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_l
 		*E = TextualInter::parse_pair(ilp->line, eloc, IBM, InterTypes::unchecked(),
 			value_text, &val);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 @h Writing to textual Inter syntax.
 
 =
-void InstanceInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int InstanceInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *instance_s = InstanceInstruction::instance(P);
 	WRITE("instance (");
 	TextualInter::write_symbol_from(OUT, P, TYPE_INST_IFLD);
 	WRITE(") %S = ", InterSymbol::identifier(instance_s));
 	TextualInter::write_pair(OUT, P, InterValuePairs::get(P, VAL1_INST_IFLD));
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Nop Construct.w b/inter/bytecode-module/Chapter 4/The Nop Construct.w
index c9756ae39..2ec18dc2e 100644
--- a/inter/bytecode-module/Chapter 4/The Nop Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Nop Construct.w	
@@ -45,6 +45,7 @@ The |nop| construct is not expressible in textual Inter, but can be printed out
 all the same, purely so that a stack backtrace will show it.
 
 =
-void NopInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int NopInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("nop");
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 4/The Package Construct.w b/inter/bytecode-module/Chapter 4/The Package Construct.w
index b83a0ddc4..4d77bd152 100644
--- a/inter/bytecode-module/Chapter 4/The Package Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Package Construct.w	
@@ -104,10 +104,11 @@ inter_error_message *PackageInstruction::new_inner(inter_bookmark *IBM,
 	return NULL;
 }
 
-void PackageInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int PackageInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[PID_PACKAGE_IFLD] = grid[P->W.instruction[PID_PACKAGE_IFLD]];
 	P->W.instruction[SYMBOLS_PACKAGE_IFLD] = grid[P->W.instruction[SYMBOLS_PACKAGE_IFLD]];
+	return 0;
 }
 
 @ Verification begins with sanity checks, but then does something crucial: makes
@@ -117,24 +118,24 @@ that will be done already -- but not if the instruction has been loaded from
 a binary Inter file.
 
 =
-void PackageInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int PackageInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	*E = VerifyingInter::SID_field(owner, P, DEFN_PACKAGE_IFLD, PACKAGE_IST);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::TID_field(owner, P, TYPE_PACKAGE_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::GSID_field(P, PTYPE_PACKAGE_IFLD, PACKAGETYPE_IST);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::symbols_table_field(owner, P, SYMBOLS_PACKAGE_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::package_field(owner, P, PID_PACKAGE_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 
 	inter_package *pack = Inode::ID_to_package(P, P->W.instruction[PID_PACKAGE_IFLD]);
 	if (pack) pack->package_head = P; else internal_error("no package in PID field");
 }
 
-void PackageInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int PackageInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	if (InterPackage::is_a_function_body(PackageInstruction::at_this_head(P))) {
 		LOOP_THROUGH_INTER_CHILDREN(C, P) {
@@ -143,7 +144,7 @@ void PackageInstruction::verify_children(inter_construct *IC, inter_tree_node *P
 				(C->W.instruction[0] != CODE_IST) &&
 				(C->W.instruction[0] != COMMENT_IST)) {
 				*E = Inode::error(C, I"instruction not permitted at the top level", NULL);
-				return;
+				return 0;
 			}
 		}
 	}
@@ -152,7 +153,7 @@ void PackageInstruction::verify_children(inter_construct *IC, inter_tree_node *P
 @h Creating from textual Inter syntax.
 
 =
-void PackageInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int PackageInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	inter_symbol *ptype_name = LargeScale::package_type(InterBookmark::tree(IBM), ilp->mr.exp[1]);
 
@@ -170,18 +171,20 @@ void PackageInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_li
 		if (*E == NULL) InterBookmark::move_into_package(IBM, pack);
 	}
 	Regexp::dispose_of(&mr);
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void PackageInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PackageInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_package *pack = PackageInstruction::at_this_head(P);
 	inter_symbol *ptype_name =
 		InterSymbolsTable::global_symbol_from_ID_at_node(P, PTYPE_PACKAGE_IFLD);
 	WRITE("package ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_PACKAGE_IFLD);
 	WRITE("%S %S", InterPackage::name(pack), InterSymbol::identifier(ptype_name));
+	return 0;
 }
 
 @ With the addendum of writing out the pseudo-constructs |plug| and |socket|
diff --git a/inter/bytecode-module/Chapter 4/The PackageType Construct.w b/inter/bytecode-module/Chapter 4/The PackageType Construct.w
index 0f63e4951..7a10394ba 100644
--- a/inter/bytecode-module/Chapter 4/The PackageType Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The PackageType Construct.w	
@@ -37,11 +37,11 @@ inter_error_message *PackageTypeInstruction::new(inter_bookmark *IBM, inter_symb
 @h Creating from textual Inter syntax.
 
 =
-void PackageTypeInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PackageTypeInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	inter_symbol *ptype_name =
 		TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), ilp->mr.exp[0], E);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = PackageTypeInstruction::new(IBM, ptype_name, (inter_ti) ilp->indent_level, eloc);
 }
@@ -49,7 +49,8 @@ void PackageTypeInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void PackageTypeInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PackageTypeInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *ptype_name = InterSymbolsTable::symbol_from_ID_at_node(P, DEFN_PTYPE_IFLD);
 	WRITE("packagetype %S", InterSymbol::identifier(ptype_name));
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 4/The Permission Construct.w b/inter/bytecode-module/Chapter 4/The Permission Construct.w
index 6ff0d64b6..a4dc41a4d 100644
--- a/inter/bytecode-module/Chapter 4/The Permission Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Permission Construct.w	
@@ -52,19 +52,19 @@ But the process is not entirely passive because we also use verification to add
 the new permission to the list for the owner and for the property.
 
 =
-void PermissionInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PermissionInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::SID_field(owner, P, PROP_PERM_IFLD, PROPERTY_IST);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::POID_field(owner, P, OWNER_PERM_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	if (P->W.instruction[STORAGE_PERM_IFLD]) {
 		*E = VerifyingInter::SID_field(owner, P, STORAGE_PERM_IFLD, CONSTANT_IST);
-		if (*E) return;
+		if (*E) return 0;
 	}
 }
 
-void PermissionInstruction::xref(inter_construct *IC, inter_tree_node *P,
+int PermissionInstruction::xref(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	inter_symbol *prop_s = InterSymbolsTable::symbol_from_ID_at_node(P, PROP_PERM_IFLD);
 	inter_symbol *owner_s = InterSymbolsTable::symbol_from_ID_at_node(P, OWNER_PERM_IFLD);
@@ -73,31 +73,31 @@ void PermissionInstruction::xref(inter_construct *IC, inter_tree_node *P,
 	if (TypenameInstruction::is(owner_s)) {
 		if (InterTypes::is_enumerated(InterTypes::from_type_name(owner_s)) == FALSE) {
 			*E = Inode::error(P, I"property permission for non-enumerated kind", NULL);
-			return;
+			return 0;
 		}
 		FL = TypenameInstruction::permissions_list(owner_s);
 	} else if (InstanceInstruction::is(owner_s)) {
 		FL = InstanceInstruction::permissions_list(owner_s);
 	} else {
 		*E = Inode::error(P, I"property permission for impossible owner", NULL);
-		return;
+		return 0;
 	}
 	if (FL == NULL) {
 		*E = Inode::error(P, I"property permission for owner without list", NULL);
-		return;
+		return 0;
 	}
 	inter_tree_node *X;
 	LOOP_THROUGH_INTER_NODE_LIST(X, FL)
 		if (InterSymbolsTable::symbol_from_ID_at_node(X, PROP_PERM_IFLD) == prop_s) {
 			*E = Inode::error(P, I"duplicate permission", InterSymbol::identifier(prop_s));
-			return;
+			return 0;
 		}
 	InterNodeList::add(FL, P);
 
 	FL = Inode::ID_to_frame_list(P, PropertyInstruction::permissions_list(prop_s));
 	if (FL == NULL) {
 		*E = Inode::error(P, I"property permission for property without list", NULL);
-		return;
+		return 0;
 	}
 	InterNodeList::add(FL, P);
 }
@@ -105,7 +105,7 @@ void PermissionInstruction::xref(inter_construct *IC, inter_tree_node *P,
 @h Creating from textual Inter syntax.
 
 =
-void PermissionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PermissionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *owner_name = ilp->mr.exp[0];
 	text_stream *prop_name = ilp->mr.exp[1];
@@ -113,14 +113,14 @@ void PermissionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 
 	inter_symbol *prop_s =
 		TextualInter::find_symbol(IBM, eloc, prop_name, PROPERTY_IST, E);
-	if (*E) return;
+	if (*E) return 0;
 	inter_symbol *owner_s =
 		PropertyValueInstruction::parse_owner(eloc, InterBookmark::scope(IBM), owner_name, E);
-	if (*E) return;
+	if (*E) return 0;
 	inter_symbol *store = NULL;
 	if (Str::len(storage_name) > 0) {
 		store = TextualInter::find_symbol(IBM, eloc, storage_name, CONSTANT_IST, E);
-		if (*E) return;
+		if (*E) return 0;
 	}
 
 	*E = PermissionInstruction::new(IBM, prop_s, owner_s, store,
@@ -130,7 +130,7 @@ void PermissionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void PermissionInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PermissionInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("permission for ");
 	TextualInter::write_symbol_from(OUT, P, OWNER_PERM_IFLD);
 	WRITE(" to have ");
@@ -139,6 +139,7 @@ void PermissionInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree
 		WRITE(" ");
 		TextualInter::write_symbol_from(OUT, P, STORAGE_PERM_IFLD);
 	}
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Pragma Construct.w b/inter/bytecode-module/Chapter 4/The Pragma Construct.w
index f5bdd570e..77bfc41fe 100644
--- a/inter/bytecode-module/Chapter 4/The Pragma Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Pragma Construct.w	
@@ -37,21 +37,22 @@ inter_error_message *PragmaInstruction::new(inter_bookmark *IBM, text_stream *ta
 	return NULL;
 }
 
-void PragmaInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int PragmaInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[TARGET_PRAGMA_IFLD] = grid[P->W.instruction[TARGET_PRAGMA_IFLD]];
 	P->W.instruction[TEXT_PRAGMA_IFLD] = grid[P->W.instruction[TEXT_PRAGMA_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void PragmaInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PragmaInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::text_field(owner, P, TARGET_PRAGMA_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::text_field(owner, P, TEXT_PRAGMA_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
@@ -59,7 +60,7 @@ Note that the target name should be an identifier-like name, without quotes;
 whereas the content is parsed as a double-quoted literal.
 
 =
-void PragmaInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int PragmaInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *target_name = ilp->mr.exp[0];
 	text_stream *content_token = ilp->mr.exp[1];
@@ -68,14 +69,16 @@ void PragmaInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_lin
 	if (*E == NULL)
 		*E = PragmaInstruction::new(IBM, target_name, raw, (inter_ti) ilp->indent_level, eloc);
 	DISCARD_TEXT(raw)
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void PragmaInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PragmaInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("pragma %S ", PragmaInstruction::target(P));
 	TextualInter::write_text(OUT, PragmaInstruction::content(P));
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Primitive Construct.w b/inter/bytecode-module/Chapter 4/The Primitive Construct.w
index c39dff65c..b7ce81578 100644
--- a/inter/bytecode-module/Chapter 4/The Primitive Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Primitive Construct.w	
@@ -71,42 +71,42 @@ inter_error_message *PrimitiveInstruction::new(inter_bookmark *IBM, inter_symbol
 @ Verification consists only of sanity checks.
 
 =
-void PrimitiveInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PrimitiveInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	if (P->W.instruction[BIP_PRIM_IFLD] >= MAX_BIPS) {
 		*E = Inode::error(P, I"primitive with impossible BIP code", NULL);
-		return;
+		return 0;
 	}
 	inter_symbol *prim_name = PrimitiveInstruction::primitive(P);
 	if ((prim_name == NULL) ||
 		(Str::get_first_char(InterSymbol::identifier(prim_name)) != '!')) {
 		*E = Inode::error(P, I"primitive name not beginning with '!'", NULL);
-		return;
+		return 0;
 	}
 	int voids = 0, args = 0;
 	for (int i=SIGNATURE_PRIM_IFLD; i<P->W.extent-1; i++) {
 		inter_ti prim_cat = P->W.instruction[i];
 		if (PrimitiveInstruction::category_is_valid(prim_cat) == FALSE) {
 			*E = Inode::error(P, I"unknown primitive category", NULL);
-			return;
+			return 0;
 		}
 		if (prim_cat == 0) voids++;
 		args++;
 	}
 	if ((voids > 1) || ((voids == 1) && (args > 1))) {
 		*E = Inode::error(P, I"if used on the left, 'void' must be the only argument", NULL);
-		return;
+		return 0;
 	}
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void PrimitiveInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PrimitiveInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	inter_symbol *prim_name =
 		TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), ilp->mr.exp[0], E);
-	if (*E) return;
+	if (*E) return 0;
 	*E = PrimitiveInstruction::new(IBM, prim_name, ilp->mr.exp[1], ilp->mr.exp[2],
 		(inter_ti) ilp->indent_level, eloc);
 }
@@ -114,7 +114,7 @@ void PrimitiveInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void PrimitiveInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PrimitiveInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *prim_name = InterSymbolsTable::symbol_from_ID_at_node(P, DEFN_PRIM_IFLD);
 	WRITE("primitive %S", InterSymbol::identifier(prim_name));
 	for (int i=SIGNATURE_PRIM_IFLD; i<P->W.extent-1; i++) {
@@ -124,6 +124,7 @@ void PrimitiveInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_
 	if (SIGNATURE_PRIM_IFLD == P->W.extent-1) WRITE(" void");
 	WRITE(" -> ");
 	PrimitiveInstruction::write_category(OUT, P->W.instruction[P->W.extent-1]);
+	return 0;
 }
 
 @h The BIP.
diff --git a/inter/bytecode-module/Chapter 4/The Property Construct.w b/inter/bytecode-module/Chapter 4/The Property Construct.w
index 4324bbfce..2adcf63a7 100644
--- a/inter/bytecode-module/Chapter 4/The Property Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Property Construct.w	
@@ -42,26 +42,27 @@ inter_error_message *PropertyInstruction::new(inter_bookmark *IBM, inter_symbol
 	return NULL;
 }
 
-void PropertyInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int PropertyInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[PERM_LIST_PROP_IFLD] = grid[P->W.instruction[PERM_LIST_PROP_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void PropertyInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PropertyInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TYPE_PROP_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::node_list_field(owner, P, PERM_LIST_PROP_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void PropertyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PropertyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = NULL, *name_text = ilp->mr.exp[0];
 	match_results mr = Regexp::create_mr();
@@ -73,7 +74,7 @@ void PropertyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_type prop_type = InterTypes::unchecked();
 	if (*E == NULL) prop_type = InterTypes::parse_simple(scope, eloc, type_text, E);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 	SymbolAnnotation::copy_set_to_symbol(&(ilp->set), prop_name);
 	*E = PropertyInstruction::new(IBM, prop_name, prop_type,
@@ -83,11 +84,12 @@ void PropertyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void PropertyInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PropertyInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *prop_name = InterSymbolsTable::symbol_from_ID_at_node(P, DEFN_PROP_IFLD);
 	WRITE("property ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_PROP_IFLD);
 	WRITE("%S", InterSymbol::identifier(prop_name));
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The PropertyValue Construct.w b/inter/bytecode-module/Chapter 4/The PropertyValue Construct.w
index db610d423..ed8022229 100644
--- a/inter/bytecode-module/Chapter 4/The PropertyValue Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The PropertyValue Construct.w	
@@ -43,30 +43,31 @@ inter_error_message *PropertyValueInstruction::new(inter_bookmark *IBM,
 	return NULL;
 }
 
-void PropertyValueInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int PropertyValueInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	InterValuePairs::set(P, VAL1_PVAL_IFLD,
 		InterValuePairs::transpose(InterValuePairs::get(P, VAL1_PVAL_IFLD), grid, grid_extent, E));
+	return 0;
 }
 
 @ Verification begins with sanity checks, but then also adds the new property
 value to the list of properties of the owner.
 
 =
-void PropertyValueInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PropertyValueInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::SID_field(owner, P, PROP_PVAL_IFLD, PROPERTY_IST);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::POID_field(owner, P, OWNER_PVAL_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
-void PropertyValueInstruction::xref(inter_construct *IC, inter_tree_node *P,
+int PropertyValueInstruction::xref(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	inter_symbol *prop_s = PropertyValueInstruction::property(P);
 	inter_package *owner = Inode::get_package(P);
 	*E = VerifyingInter::data_pair_fields(owner, P, VAL1_PVAL_IFLD, InterTypes::unchecked());
-	if (*E) return;
+	if (*E) return 0;
 	inter_symbol *owner_s = PropertyValueInstruction::owner(P);
 
 	if (PropertyValueInstruction::permitted(owner_s, prop_s) == FALSE) {
@@ -74,7 +75,7 @@ void PropertyValueInstruction::xref(inter_construct *IC, inter_tree_node *P,
 		WRITE_TO(err, "no permission for '%S' to have this property",
 			InterSymbol::identifier(owner_s));
 		*E = Inode::error(P, err, InterSymbol::identifier(prop_s));
-		return;
+		return 0;
 	}
 
 	inter_node_list *FL;
@@ -86,7 +87,7 @@ void PropertyValueInstruction::xref(inter_construct *IC, inter_tree_node *P,
 	LOOP_THROUGH_INTER_NODE_LIST(X, FL)
 		if (PropertyValueInstruction::property(X) == prop_s) {
 			*E = Inode::error(P, I"duplicate property value", NULL);
-			return;
+			return 0;
 		}
 
 	InterNodeList::add(FL, P);
@@ -116,19 +117,19 @@ int PropertyValueInstruction::permitted(inter_symbol *owner, inter_symbol *prop_
 @h Creating from textual Inter syntax.
 
 =
-void PropertyValueInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PropertyValueInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *prop_name = ilp->mr.exp[0], *owner_name = ilp->mr.exp[1];
 	inter_symbol *prop_s = TextualInter::find_symbol(IBM, eloc, prop_name, PROPERTY_IST, E);
-	if (*E) return;
+	if (*E) return 0;
 	inter_symbol *owner_s = PropertyValueInstruction::parse_owner(eloc,
 		InterBookmark::scope(IBM), owner_name, E);
-	if (*E) return;
+	if (*E) return 0;
 
 	inter_type val_type = InterTypes::of_symbol(prop_s);
 	inter_pair val = InterValuePairs::undef();
 	*E = TextualInter::parse_pair(ilp->line, eloc, IBM, val_type, ilp->mr.exp[2], &val);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = PropertyValueInstruction::new(IBM, prop_s, owner_s, val,
 		(inter_ti) ilp->indent_level, eloc);
@@ -158,13 +159,14 @@ inter_symbol *PropertyValueInstruction::parse_owner(inter_error_location *eloc,
 @h Writing to textual Inter syntax.
 
 =
-void PropertyValueInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int PropertyValueInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("propertyvalue ");
 	TextualInter::write_symbol_from(OUT, P, PROP_PVAL_IFLD);
 	WRITE(" of ");
 	TextualInter::write_symbol_from(OUT, P, OWNER_PVAL_IFLD);
 	WRITE(" = ");
 	TextualInter::write_pair(OUT, P, PropertyValueInstruction::value(P));
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Typename Construct.w b/inter/bytecode-module/Chapter 4/The Typename Construct.w
index 79fa10a02..25fec07cc 100644
--- a/inter/bytecode-module/Chapter 4/The Typename Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Typename Construct.w	
@@ -60,65 +60,66 @@ inter_error_message *TypenameInstruction::new(inter_bookmark *IBM, inter_symbol
 	return NULL;
 }
 
-void TypenameInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int TypenameInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[PERM_LIST_TYPENAME_IFLD] = grid[P->W.instruction[PERM_LIST_TYPENAME_IFLD]];
 	P->W.instruction[PLIST_TYPENAME_IFLD] = grid[P->W.instruction[PLIST_TYPENAME_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void TypenameInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int TypenameInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	inter_symbol *typename_s = TypenameInstruction::typename(P);
 	if ((P->W.instruction[ENUM_RANGE_TYPENAME_IFLD] > 0) &&
 		(InterTypes::is_enumerated(InterTypes::from_type_name(typename_s)) == FALSE)) {
 		*E = Inode::error(P, I"spurious extent in non-enumeration", NULL);
-		return;
+		return 0;
 	}
 	if (P->W.instruction[SUPER_TYPENAME_IFLD] != 0) {
 		*E = VerifyingInter::SID_field(owner, P, SUPER_TYPENAME_IFLD, TYPENAME_IST);
-		if (*E) return;
+		if (*E) return 0;
 		inter_symbol *super_s = TypenameInstruction::super(typename_s);
 		if (InterTypes::is_enumerated(InterTypes::from_type_name(super_s)) == FALSE) {
 			*E = Inode::error(P, I"subtype of nonenumerated type", NULL);
-			return;
+			return 0;
 		}
 	}
 	*E = VerifyingInter::node_list_field(owner, P, PERM_LIST_TYPENAME_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::node_list_field(owner, P, PLIST_TYPENAME_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::constructor_field(P, CONSTRUCTOR_TYPENAME_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	inter_type type =
 		InterTypes::from_constructor_code(TypenameInstruction::constructor(typename_s));
 	int arity = P->W.extent - OPERANDS_TYPENAME_IFLD;
 	for (int i=0; i<arity; i++) {
 		*E = VerifyingInter::TID_field(owner, P, OPERANDS_TYPENAME_IFLD + i);
-		if (*E) return;
+		if (*E) return 0;
 	}
 	if (InterTypes::arity_is_possible(type, arity) == FALSE) {
 		text_stream *err = Str::new();
 		WRITE_TO(err, "typename definition has arity %d, which is impossible for ", arity);
 		InterTypes::write_type(err, type);
 		*E = Inode::error(P, err, NULL);
-		return;
+		return 0;
 	}
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void TypenameInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int TypenameInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *typename_text = ilp->mr.exp[0];
 	text_stream *operator_text = ilp->mr.exp[1];
 	text_stream *defn_text = ilp->mr.exp[2];
 	inter_symbol *symb = 
 		TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), typename_text, E);
-	if (*E) return;
+	if (*E) return 0;
 
 	inter_semisimple_type_description parsed_description;
 	InterTypes::initialise_isstd(&parsed_description);
@@ -129,7 +130,7 @@ void TypenameInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 		if ((*E == NULL) &&
 			(InterTypes::is_enumerated(InterTypes::from_type_name(super_s)) == FALSE)) {
 				*E = InterErrors::quoted(I"not a type which can have subtypes", defn_text, eloc);
-				return;
+				return 0;
 		}
 		parsed_description.constructor_code = ENUM_ITCONC;
 		parsed_description.arity = 0;
@@ -150,7 +151,7 @@ void TypenameInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void TypenameInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int TypenameInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *typename_s = TypenameInstruction::typename(P);
 	WRITE("typename %S ", InterSymbol::identifier(typename_s));
 	inter_symbol *super = TypenameInstruction::super(typename_s);
@@ -161,6 +162,7 @@ void TypenameInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_n
 		WRITE("= ");
 		InterTypes::write_typename_definition(OUT, typename_s);
 	}
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 4/The Variable Construct.w b/inter/bytecode-module/Chapter 4/The Variable Construct.w
index 4c9ae8c2d..c58ee9a66 100644
--- a/inter/bytecode-module/Chapter 4/The Variable Construct.w	
+++ b/inter/bytecode-module/Chapter 4/The Variable Construct.w	
@@ -42,28 +42,29 @@ inter_error_message *VariableInstruction::new(inter_bookmark *IBM, inter_symbol
 	return NULL;
 }
 
-void VariableInstruction::transpose(inter_construct *IC, inter_tree_node *P,
+int VariableInstruction::transpose(inter_construct *IC, inter_tree_node *P,
 	inter_ti *grid, inter_ti grid_extent, inter_error_message **E) {
 	InterValuePairs::set(P, VAL1_PVAL_IFLD,
 		InterValuePairs::transpose(InterValuePairs::get(P, VAL1_PVAL_IFLD), grid, grid_extent, E));
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void VariableInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int VariableInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TYPE_VAR_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	inter_type type = InterTypes::from_TID_in_field(P, TYPE_VAR_IFLD);
 	*E = VerifyingInter::data_pair_fields(owner, P, VAL1_VAR_IFLD, type);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void VariableInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int VariableInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = NULL, *name_text = ilp->mr.exp[0];
 	match_results mr = Regexp::create_mr();
@@ -76,13 +77,13 @@ void VariableInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	if (*E == NULL)
 		var_s = TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), name_text, E);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 	SymbolAnnotation::copy_set_to_symbol(&(ilp->set), var_s);
 
 	inter_pair val = InterValuePairs::undef();
 	*E = TextualInter::parse_pair(ilp->line, eloc, IBM, var_type, ilp->mr.exp[1], &val);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = VariableInstruction::new(IBM, var_s, var_type, val,
 		(inter_ti) ilp->indent_level, eloc);
@@ -91,12 +92,13 @@ void VariableInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void VariableInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int VariableInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *var_s = VariableInstruction::variable(P);
 	WRITE("variable ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_VAR_IFLD);
 	WRITE("%S = ", InterSymbol::identifier(var_s));
 	TextualInter::write_pair(OUT, P, VariableInstruction::value(P));
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 5/The Assembly Construct.w b/inter/bytecode-module/Chapter 5/The Assembly Construct.w
index 5659cab3f..2741acc28 100644
--- a/inter/bytecode-module/Chapter 5/The Assembly Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Assembly Construct.w	
@@ -47,19 +47,19 @@ is required to be one of these:
 @e ASM_NEG_RFALSE_ASMMARKER
 
 =
-void AssemblyInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int AssemblyInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	inter_ti which = P->W.instruction[WHICH_ASSEMBLY_IFLD];
 	if ((which == 0) || (which > ASM_NEG_RFALSE_ASMMARKER)) {
 		*E = Inode::error(P, I"bad assembly marker code", NULL);
-		return;
+		return 0;
 	}
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void AssemblyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int AssemblyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *marker_text = ilp->mr.exp[0];
 	inter_ti which = 0;
@@ -70,7 +70,7 @@ void AssemblyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	else if (Str::eq(marker_text, I"branch_if_false"))       which = ASM_NEG_ASMMARKER;
 	else if (Str::eq(marker_text, I"return_true_if_false"))  which = ASM_NEG_RTRUE_ASMMARKER;
 	else if (Str::eq(marker_text, I"return_false_if_false")) which = ASM_NEG_RFALSE_ASMMARKER;
-	else { *E = InterErrors::plain(I"unrecognised 'assembly' marker", eloc); return; }
+	else { *E = InterErrors::plain(I"unrecognised 'assembly' marker", eloc); return 0; }
 
 	*E = AssemblyInstruction::new(IBM, which, (inter_ti) ilp->indent_level, eloc);
 }
@@ -78,7 +78,7 @@ void AssemblyInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void AssemblyInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int AssemblyInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_ti which = AssemblyInstruction::which_marker(P);
 	switch (which) {
 		case ASM_ARROW_ASMMARKER:      WRITE("assembly store_to"); break;
@@ -89,6 +89,7 @@ void AssemblyInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_n
 		case ASM_NEG_RTRUE_ASMMARKER:  WRITE("assembly return_true_if_false"); break;
 		case ASM_NEG_RFALSE_ASMMARKER: WRITE("assembly return_false_if_false"); break;
 	}
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Cast Construct.w b/inter/bytecode-module/Chapter 5/The Cast Construct.w
index 2bc4fca63..924694b3e 100644
--- a/inter/bytecode-module/Chapter 5/The Cast Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Cast Construct.w	
@@ -42,15 +42,15 @@ inter_error_message *CastInstruction::new(inter_bookmark *IBM, inter_type from_t
 @ Verification consists only of sanity checks.
 
 =
-void CastInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int CastInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TO_KIND_CAST_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	*E = VerifyingInter::TID_field(owner, P, FROM_KIND_CAST_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 }
 
-void CastInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int CastInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	int arity_as_invoked = 0;
 	LOOP_THROUGH_INTER_CHILDREN(C, P) {
@@ -60,28 +60,28 @@ void CastInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 			(C->W.instruction[0] != EVALUATION_IST) &&
 			(C->W.instruction[0] != CAST_IST)) {
 			*E = Inode::error(P, I"only a value can be under a cast", NULL);
-			return;
+			return 0;
 		}
 	}
 	if (arity_as_invoked != 1) {
 		*E = Inode::error(P, I"a cast should have exactly one child", NULL);
-		return;
+		return 0;
 	}
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void CastInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int CastInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *to_text = ilp->mr.exp[0];
 	text_stream *from_text = ilp->mr.exp[1];
 	
 	inter_symbols_table *T = InterBookmark::scope(IBM);
 	inter_type from_type = InterTypes::parse_simple(T, eloc, from_text, E);
-	if (*E) return;
+	if (*E) return 0;
 	inter_type to_type = InterTypes::parse_simple(T, eloc, to_text, E);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = CastInstruction::new(IBM, from_type, to_type, (inter_ti) ilp->indent_level, eloc);
 }
@@ -89,9 +89,10 @@ void CastInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_
 @h Writing to textual Inter syntax.
 
 =
-void CastInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int CastInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("cast ");
 	TextualInter::write_compulsory_type_marker(OUT, P, TO_KIND_CAST_IFLD);
 	WRITE(" <- ");
 	TextualInter::write_compulsory_type_marker(OUT, P, FROM_KIND_CAST_IFLD);
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 5/The Code Construct.w b/inter/bytecode-module/Chapter 5/The Code Construct.w
index 2b832b1b9..a662a0f64 100644
--- a/inter/bytecode-module/Chapter 5/The Code Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Code Construct.w	
@@ -36,7 +36,7 @@ inter_error_message *CodeInstruction::new(inter_bookmark *IBM, int level,
 @ Verification consists only of sanity checks.
 
 =
-void CodeInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int CodeInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	LOOP_THROUGH_INTER_CHILDREN(C, P) {
 		if ((C->W.instruction[0] != INV_IST) &&
@@ -47,7 +47,7 @@ void CodeInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 			(C->W.instruction[0] != COMMENT_IST) &&
 			(C->W.instruction[0] != NOP_IST)) {
 			*E = Inode::error(C, I"only executable matter can be below a code", NULL);
-			return;
+			return 0;
 		}
 	}
 }
@@ -55,14 +55,16 @@ void CodeInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 @h Creating from textual Inter syntax.
 
 =
-void CodeInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int CodeInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	*E = CodeInstruction::new(IBM, ilp->indent_level, eloc);
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void CodeInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int CodeInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("code");
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 5/The Evaluation Construct.w b/inter/bytecode-module/Chapter 5/The Evaluation Construct.w
index 200e730f9..9fa7bbb8e 100644
--- a/inter/bytecode-module/Chapter 5/The Evaluation Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Evaluation Construct.w	
@@ -36,7 +36,7 @@ inter_error_message *EvaluationInstruction::new(inter_bookmark *IBM, int level,
 @ Verification consists only of sanity checks.
 
 =
-void EvaluationInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int EvaluationInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	LOOP_THROUGH_INTER_CHILDREN(C, P) {
 		if ((C->W.instruction[0] != INV_IST) &&
@@ -45,7 +45,7 @@ void EvaluationInstruction::verify_children(inter_construct *IC, inter_tree_node
 			(C->W.instruction[0] != LABEL_IST) &&
 			(C->W.instruction[0] != EVALUATION_IST)) {
 			*E = Inode::error(C, I"only a value can be below an evaluation", NULL);
-			return;
+			return 0;
 		}
 	}
 }
@@ -53,14 +53,16 @@ void EvaluationInstruction::verify_children(inter_construct *IC, inter_tree_node
 @h Creating from textual Inter syntax.
 
 =
-void EvaluationInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int EvaluationInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	*E = EvaluationInstruction::new(IBM, ilp->indent_level, eloc);
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void EvaluationInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int EvaluationInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("evaluation");
+	return 0;
 }
diff --git a/inter/bytecode-module/Chapter 5/The Inv Construct.w b/inter/bytecode-module/Chapter 5/The Inv Construct.w
index 6667060d4..1d6a2dbc3 100644
--- a/inter/bytecode-module/Chapter 5/The Inv Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Inv Construct.w	
@@ -73,29 +73,30 @@ inter_error_message *InvInstruction::new_assembly(inter_bookmark *IBM,
 	return NULL;
 }
 
-void InvInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int InvInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	if (P->W.instruction[METHOD_INV_IFLD] == OPCODE_INVMETH)
 		P->W.instruction[INVOKEE_INV_IFLD] = grid[P->W.instruction[INVOKEE_INV_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void InvInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int InvInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	switch (P->W.instruction[METHOD_INV_IFLD]) {
 		case PRIMITIVE_INVMETH:
 			*E = VerifyingInter::GSID_field(P, INVOKEE_INV_IFLD, PRIMITIVE_IST);
-			if (*E) return;
+			if (*E) return 0;
 			break;
 		case OPCODE_INVMETH:
 			*E = VerifyingInter::text_field(owner, P, INVOKEE_INV_IFLD);
-			if (*E) return;
+			if (*E) return 0;
 			break;
 		case FUNCTION_INVMETH:
 			*E = VerifyingInter::SID_field(owner, P, INVOKEE_INV_IFLD, INVALID_IST);
-			if (*E) return;
+			if (*E) return 0;
 			break;
 		default:
 			*E = Inode::error(P, I"bad invocation method", NULL);
@@ -118,7 +119,7 @@ example, if the signature of the primitive invoked at |P| is |ref val val -> voi
 then |P| needs to have exactly three children, of categories |ref|, |val|, |val|.
 
 =
-void InvInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int InvInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	if (P->W.instruction[METHOD_INV_IFLD] == PRIMITIVE_INVMETH) {
 		int arity_as_invoked=0;
@@ -143,7 +144,7 @@ void InvInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 			(C->W.instruction[0] != COMMENT_IST) &&
 			(C->W.instruction[0] != ASSEMBLY_IST)) {
 			*E = Inode::error(P, I"forbidden instruction under an inv", NULL);
-			return;
+			return 0;
 		}
 		if (P->W.instruction[METHOD_INV_IFLD] != OPCODE_INVMETH)
 			@<Check that the category of the child matches what is expected@>;
@@ -177,7 +178,7 @@ number of arguments, then we change it to the version with the right arity.
 			(prim_s)?(InterSymbol::identifier(prim_s)):I"<unknown>",
 			PrimitiveInstruction::arity(prim_s), arity_as_invoked);
 		*E = Inode::error(P, err, NULL);
-		return;
+		return 0;
 	}
 
 @ In effect, this is a limited form of typechecker for invocations. For example,
@@ -202,7 +203,7 @@ child (operand 0) to be a |ref|, and its second (operand 1) to be a |val|.
 			PrimitiveInstruction::cat_name(cat_needed),
 			PrimitiveInstruction::cat_name(cat_found));
 		*E = Inode::error(C, err, NULL);
-		return;
+		return 0;
 	}
 
 @ So this is the category for the value produced (if any) by an instruction.
@@ -231,7 +232,7 @@ inter_ti InvInstruction::evaluated_category(inter_tree_node *P) {
 @h Creating from textual Inter syntax.
 
 =
-void InvInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp, 
+int InvInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp, 
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *invoked_text = ilp->mr.exp[0];
 	switch (Str::get_first_char(invoked_text)) {
@@ -239,6 +240,7 @@ void InvInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 		case '@': @<This is an assembly opcode invocation@>;
 		default: @<This is a function call invocation@>;
 	}
+	return 0;
 }
 
 @<This is a primitive invocation@> =
@@ -250,15 +252,15 @@ void InvInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 			&(I->site.strdata.package_types_bookmark), invoked_text);
 		if (prim_s == NULL) {
 			*E = InterErrors::quoted(I"'inv' on undeclared primitive", invoked_text, eloc);
-			return;
+			return 0;
 		}
 	}
 	*E = InvInstruction::new_primitive(IBM, prim_s, (inter_ti) ilp->indent_level, eloc);
-	return;
+	return 0;
 
 @<This is an assembly opcode invocation@> =
 	*E = InvInstruction::new_assembly(IBM, invoked_text, (inter_ti) ilp->indent_level, eloc);
-	return;
+	return 0;
 
 @<This is a function call invocation@> =
 	inter_symbol *function_s = TextualInter::find_symbol(IBM, eloc, invoked_text, 0, E);
@@ -268,12 +270,12 @@ void InvInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 		*E = InvInstruction::new_function_call(IBM, function_s,
 			(inter_ti) ilp->indent_level, eloc);
 	}
-	return;
+	return 0;
 
 @h Writing to textual Inter syntax.
 
 =
-void InvInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int InvInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	switch (InvInstruction::method(P)) {
 		case PRIMITIVE_INVMETH:
 			WRITE("inv %S", InterSymbol::identifier(InvInstruction::primitive(P)));
@@ -286,6 +288,7 @@ void InvInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *
 			TextualInter::write_symbol_from(OUT, P, INVOKEE_INV_IFLD);
 			break;
 	}
+	return 0;
 }
 
 @h Access functions.
diff --git a/inter/bytecode-module/Chapter 5/The Lab Construct.w b/inter/bytecode-module/Chapter 5/The Lab Construct.w
index bbf0bb21e..ea22ad7ae 100644
--- a/inter/bytecode-module/Chapter 5/The Lab Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Lab Construct.w	
@@ -40,12 +40,12 @@ customary call to //VerifyingInter::SID_field// on |LABEL_LAB_IFLD| for timing
 reasons: it may refer to a symbol not yet defined.
 
 =
-void LabInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int LabInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	inter_symbol *label = LabInstruction::label_symbol(P);
 	if (InterSymbol::is_label(label) == FALSE) {
 		*E = Inode::error(P, I"no such label", NULL);
-		return;
+		return 0;
 	}
 }
 
@@ -55,19 +55,19 @@ label it refers to; so if we have an unknown label name, we create it as a label
 in expectation that the position will be declared in a future instruction.
 
 =
-void LabInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int LabInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *label_name = ilp->mr.exp[0];
 	inter_symbol *label_s =
 		InterSymbolsTable::symbol_from_name(InterBookmark::scope(IBM), label_name);
 	if (label_s == NULL) {
 		label_s = TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), label_name, E);
-		if (*E) return;
+		if (*E) return 0;
 		InterSymbol::make_label(label_s);
 	}
 	if (InterSymbol::is_label(label_s) == FALSE) {
 		*E = InterErrors::plain(I"not a label", eloc);
-		return;
+		return 0;
 	}
 	*E = LabInstruction::new(IBM, label_s, (inter_ti) ilp->indent_level, eloc);
 }
@@ -75,9 +75,10 @@ void LabInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 @h Writing to textual Inter syntax.
 
 =
-void LabInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int LabInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *label_s = LabInstruction::label_symbol(P);
 	WRITE("lab %S", InterSymbol::identifier(label_s));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Label Construct.w b/inter/bytecode-module/Chapter 5/The Label Construct.w
index 8b2deef87..52d1d811d 100644
--- a/inter/bytecode-module/Chapter 5/The Label Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Label Construct.w	
@@ -40,13 +40,13 @@ inter_error_message *LabelInstruction::new(inter_bookmark *IBM, inter_symbol *la
 @ Verification consists only of sanity checks.
 
 =
-void LabelInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int LabelInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	inter_symbol *lab_name = LabelInstruction::label_symbol(P);
 	if (InterSymbol::is_label(lab_name) == FALSE) {
 		*E = Inode::error(P, I"not a label",
 			(lab_name)?(InterSymbol::identifier(lab_name)):NULL);
-		return;
+		return 0;
 	}
 }
 
@@ -55,17 +55,17 @@ Note that a |.LABEL| can occur either before or after it is used in |lab| instru
 so that the label name might already have been created: see //LabInstruction::read//.
 
 =
-void LabelInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int LabelInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *label_name = ilp->mr.exp[0];
 	inter_symbol *label_s =
 		InterSymbolsTable::symbol_from_name(InterBookmark::scope(IBM), label_name);
 	if (label_s == NULL) {
 		label_s = TextualInter::new_symbol(eloc, InterBookmark::scope(IBM), label_name, E);
-		if (*E) return;
+		if (*E) return 0;
 	} else if (InterSymbol::is_defined(label_s)) {
 		*E = InterErrors::plain(I"label_s defined in function once already", eloc);
-		return;
+		return 0;
 	}
 	InterSymbol::make_label(label_s);
 	*E = LabelInstruction::new(IBM, label_s, (inter_ti) ilp->indent_level, eloc);
@@ -74,9 +74,10 @@ void LabelInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line
 @h Writing to textual Inter syntax.
 
 =
-void LabelInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int LabelInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	inter_symbol *label_s = LabelInstruction::label_symbol(P);
 	WRITE("%S", InterSymbol::identifier(label_s));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Local Construct.w b/inter/bytecode-module/Chapter 5/The Local Construct.w
index 01ab1bfeb..50adbb7df 100644
--- a/inter/bytecode-module/Chapter 5/The Local Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Local Construct.w	
@@ -40,15 +40,16 @@ inter_error_message *LocalInstruction::new(inter_bookmark *IBM, inter_symbol *va
 @ Verification consists only of sanity checks.
 
 =
-void LocalInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int LocalInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TYPE_LOCAL_IFLD);
+	return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void LocalInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int LocalInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = NULL, *name_text = ilp->mr.exp[0];
 	inter_symbol *variable_s = NULL;
@@ -60,7 +61,7 @@ void LocalInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line
 	inter_type var_type = InterTypes::parse_simple(T, eloc, type_text, E);
 	if (*E == NULL) variable_s = TextualInter::new_symbol(eloc, T, name_text, E);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 	InterSymbol::make_local(variable_s);
 	SymbolAnnotation::copy_set_to_symbol(&(ilp->set), variable_s);
@@ -71,11 +72,12 @@ void LocalInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line
 @h Writing to textual Inter syntax.
 
 =
-void LocalInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int LocalInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("local ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_LOCAL_IFLD);
 	inter_symbol *variable_s = LocalInstruction::variable(P);
 	WRITE("%S", InterSymbol::identifier(variable_s));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Ref Construct.w b/inter/bytecode-module/Chapter 5/The Ref Construct.w
index dec09e401..53091a159 100644
--- a/inter/bytecode-module/Chapter 5/The Ref Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Ref Construct.w	
@@ -42,19 +42,19 @@ inter_error_message *RefInstruction::new(inter_bookmark *IBM, inter_type ref_typ
 @ Verification consists only of sanity checks.
 
 =
-void RefInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
+int RefInstruction::verify(inter_construct *IC, inter_tree_node *P, inter_package *owner,
 	inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TYPE_REF_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	inter_type type = InterTypes::from_TID_in_field(P, TYPE_REF_IFLD);
 	*E = VerifyingInter::data_pair_fields(owner, P, VAL1_REF_IFLD, type);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void RefInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int RefInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = NULL, *value_text = ilp->mr.exp[0];
 	match_results mr = Regexp::create_mr();
@@ -67,7 +67,7 @@ void RefInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 	if (*E == NULL)
 		*E = TextualInter::parse_pair(ilp->line, eloc, IBM, ref_type, value_text, &val);
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = RefInstruction::new(IBM, ref_type, ilp->indent_level, val, eloc);
 }
@@ -75,10 +75,11 @@ void RefInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 @h Writing to textual Inter syntax.
 
 =
-void RefInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int RefInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("ref ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_REF_IFLD);
 	TextualInter::write_pair(OUT, P, RefInstruction::value(P));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Reference Construct.w b/inter/bytecode-module/Chapter 5/The Reference Construct.w
index 04c0cb6c2..30d7b4a4e 100644
--- a/inter/bytecode-module/Chapter 5/The Reference Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Reference Construct.w	
@@ -36,7 +36,7 @@ inter_error_message *ReferenceInstruction::new(inter_bookmark *IBM, int level,
 @ Verification consists only of sanity checks.
 
 =
-void ReferenceInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
+int ReferenceInstruction::verify_children(inter_construct *IC, inter_tree_node *P,
 	inter_error_message **E) {
 	LOOP_THROUGH_INTER_CHILDREN(C, P) {
 		if ((C->W.instruction[0] != INV_IST) &&
@@ -45,7 +45,7 @@ void ReferenceInstruction::verify_children(inter_construct *IC, inter_tree_node
 			(C->W.instruction[0] != VAL_IST) &&
 			(C->W.instruction[0] != LABEL_IST)) {
 			*E = Inode::error(C, I"instruction cannot be referenced", NULL);
-			return;
+			return 0;
 		}
 	}
 }
@@ -53,16 +53,18 @@ void ReferenceInstruction::verify_children(inter_construct *IC, inter_tree_node
 @h Creating from textual Inter syntax.
 
 =
-void ReferenceInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int ReferenceInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	*E = ReferenceInstruction::new(IBM, ilp->indent_level, eloc);
+	return 0;
 }
 
 @h Writing to textual Inter syntax.
 
 =
-void ReferenceInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int ReferenceInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("reference");
+	return 0;
 }
 
 @h Detection.
diff --git a/inter/bytecode-module/Chapter 5/The Splat Construct.w b/inter/bytecode-module/Chapter 5/The Splat Construct.w
index 6fe82062c..4534bf2a7 100644
--- a/inter/bytecode-module/Chapter 5/The Splat Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Splat Construct.w	
@@ -40,28 +40,29 @@ inter_error_message *SplatInstruction::new(inter_bookmark *IBM, text_stream *spl
 	return NULL;
 }
 
-void SplatInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int SplatInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	P->W.instruction[MATTER_SPLAT_IFLD] = grid[P->W.instruction[MATTER_SPLAT_IFLD]];
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void SplatInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int SplatInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::text_field(owner, P, MATTER_SPLAT_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	if (SplatInstruction::plm_valid(P->W.instruction[PLM_SPLAT_IFLD]) == FALSE) {
 		*E = Inode::error(P, I"plm out of range", NULL);
-		return;
+		return 0;
 	}
 }
 
 @h Creating from textual Inter syntax.
 
 =
-void SplatInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int SplatInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *plm_text = ilp->mr.exp[0];
 	text_stream *splatter_text = ilp->mr.exp[1];
@@ -69,7 +70,7 @@ void SplatInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_ti plm = SplatInstruction::parse_plm(plm_text);
 	if (SplatInstruction::plm_valid(plm) == FALSE) {
 		*E = InterErrors::plain(I"unknown PLM code before text matter", eloc);
-		return;
+		return 0;
 	}
 	TEMPORARY_TEXT(raw)
 	*E = TextualInter::parse_literal_text(raw, splatter_text, 0, Str::len(splatter_text), eloc);
@@ -81,10 +82,11 @@ void SplatInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 @h Writing to textual Inter syntax.
 
 =
-void SplatInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int SplatInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("splat ");
 	SplatInstruction::write_plm(OUT, SplatInstruction::plm(P));
 	TextualInter::write_text(OUT, SplatInstruction::splatter(P));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 5/The Val Construct.w b/inter/bytecode-module/Chapter 5/The Val Construct.w
index 2880e6714..949580518 100644
--- a/inter/bytecode-module/Chapter 5/The Val Construct.w	
+++ b/inter/bytecode-module/Chapter 5/The Val Construct.w	
@@ -40,30 +40,31 @@ inter_error_message *ValInstruction::new(inter_bookmark *IBM, inter_type val_typ
 	return NULL;
 }
 
-void ValInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
+int ValInstruction::transpose(inter_construct *IC, inter_tree_node *P, inter_ti *grid,
 	inter_ti grid_extent, inter_error_message **E) {
 	InterValuePairs::set(P, VAL1_VAL_IFLD,
 		InterValuePairs::transpose(InterValuePairs::get(P, VAL1_VAL_IFLD),
 			grid, grid_extent, E));
+	return 0;
 }
 
 @ Verification consists only of sanity checks.
 
 =
-void ValInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int ValInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = VerifyingInter::TID_field(owner, P, TYPE_VAL_IFLD);
-	if (*E) return;
+	if (*E) return 0;
 	inter_type type = InterTypes::from_TID_in_field(P, TYPE_VAL_IFLD);
 	*E = VerifyingInter::data_pair_fields(owner, P, VAL1_VAL_IFLD, type);
-	if (*E) return;
+	if (*E) return 0;
 }
 
 @h Creating from textual Inter syntax.
 Note that a |val| can legally hold a typename as a value.
 
 =
-void ValInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int ValInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *type_text = NULL, *value_text = ilp->mr.exp[0];
 	match_results mr = Regexp::create_mr();
@@ -85,7 +86,7 @@ void ValInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 		}
 	}
 	Regexp::dispose_of(&mr);
-	if (*E) return;
+	if (*E) return 0;
 
 	*E = ValInstruction::new(IBM, val_type, ilp->indent_level, val, eloc);
 }
@@ -93,10 +94,11 @@ void ValInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_p
 @h Writing to textual Inter syntax.
 
 =
-void ValInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
+int ValInstruction::write(inter_construct *IC, OUTPUT_STREAM, inter_tree_node *P) {
 	WRITE("val ");
 	TextualInter::write_optional_type_marker(OUT, P, TYPE_VAL_IFLD);
 	TextualInter::write_pair(OUT, P, ValInstruction::value(P));
+	return 0;
 }
 
 @h Access function.
diff --git a/inter/bytecode-module/Chapter 6/The Plug Construct.w b/inter/bytecode-module/Chapter 6/The Plug Construct.w
index 249d391d6..0d6c4bd0c 100644
--- a/inter/bytecode-module/Chapter 6/The Plug Construct.w	
+++ b/inter/bytecode-module/Chapter 6/The Plug Construct.w	
@@ -15,9 +15,10 @@ void PlugInstruction::define_construct(void) {
 	InterInstruction::permit(IC, INSIDE_PLAIN_PACKAGE_ICUP);
 }
 
-void PlugInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int PlugInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = Inode::error(P, I"PLUG_IST structures cannot exist", NULL);
+	return 0;
 }
 
 @ What it does is to specify a symbol which is a plug in the current tree:
@@ -29,7 +30,7 @@ only in the case where the plug asks to wire to something existing in the curren
 tree already.
 
 =
-void PlugInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int PlugInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	text_stream *symbol_text = ilp->mr.exp[0];
 	text_stream *equate_text = ilp->mr.exp[1];
@@ -37,7 +38,7 @@ void PlugInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_tree *I = InterBookmark::tree(IBM);
 	inter_symbols_table *T = InterBookmark::scope(IBM);
 	inter_symbol *plug_s = TextualInter::new_symbol(eloc, T, symbol_text, E);
-	if (*E) return;
+	if (*E) return 0;
 	InterSymbol::make_plug(plug_s);
 
 	text_stream *equate_name = NULL;
@@ -50,7 +51,7 @@ void PlugInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 		equate_name = mr.exp[0];
 	} else {
 		Regexp::dispose_of(&mr);
-		*E = InterErrors::plain(I"bad plug syntax", eloc); return;
+		*E = InterErrors::plain(I"bad plug syntax", eloc); return 0;
 	}
 
 	if (to_name) {
diff --git a/inter/bytecode-module/Chapter 6/The Socket Construct.w b/inter/bytecode-module/Chapter 6/The Socket Construct.w
index 3c2de3128..cbab26a70 100644
--- a/inter/bytecode-module/Chapter 6/The Socket Construct.w	
+++ b/inter/bytecode-module/Chapter 6/The Socket Construct.w	
@@ -15,9 +15,10 @@ void SocketInstruction::define_construct(void) {
 	InterInstruction::permit(IC, INSIDE_PLAIN_PACKAGE_ICUP);
 }
 
-void SocketInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int SocketInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = Inode::error(P, I"SOCKET_IST structures cannot exist", NULL);
+	return 0;
 }
 
 @ What it does is to specify a symbol which is a socket in the current tree:
@@ -28,7 +29,7 @@ For how these are printed back, see //PlugInstruction::write_declaration//,
 which handles both plugs and sockets.
 
 =
-void SocketInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
+int SocketInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_line_parse *ilp,
 	inter_error_location *eloc, inter_error_message **E) {
 	text_stream *symbol_text = ilp->mr.exp[0];
 	text_stream *equate_text = ilp->mr.exp[1];
@@ -36,18 +37,18 @@ void SocketInstruction::read(inter_construct *IC, inter_bookmark *IBM, inter_lin
 	inter_tree *I = InterBookmark::tree(IBM);
 	inter_symbols_table *T = InterBookmark::scope(IBM);
 	inter_symbol *socket_s = TextualInter::new_symbol(eloc, T, symbol_text, E);
-	if (*E) return;
+	if (*E) return 0;
 
 	text_stream *equate_name = NULL;
 	match_results mr = Regexp::create_mr();
 	if (Regexp::match(&mr, equate_text, L"~~> \"(%C+)\"")) {
 		*E = InterErrors::plain(I"a socket cannot wire to a name", eloc);
-		return;
+		return 0;
 	} else if (Regexp::match(&mr, equate_text, L"~~> (%C+)")) {
 		equate_name = mr.exp[0];
 	} else {
 		Regexp::dispose_of(&mr);
-		*E = InterErrors::plain(I"bad socket syntax", eloc); return;
+		*E = InterErrors::plain(I"bad socket syntax", eloc); return 0;
 	}
 
 	inter_symbol *eq = InterSymbolsTable::URL_to_symbol(I, equate_name);
diff --git a/inter/bytecode-module/Chapter 6/The Version Construct.w b/inter/bytecode-module/Chapter 6/The Version Construct.w
index 332333458..1e62b2735 100644
--- a/inter/bytecode-module/Chapter 6/The Version Construct.w	
+++ b/inter/bytecode-module/Chapter 6/The Version Construct.w	
@@ -14,9 +14,10 @@ void VersionInstruction::define_construct(void) {
 	METHOD_ADD(IC, CONSTRUCT_VERIFY_MTID, VersionInstruction::verify);
 }
 
-void VersionInstruction::verify(inter_construct *IC, inter_tree_node *P,
+int VersionInstruction::verify(inter_construct *IC, inter_tree_node *P,
 	inter_package *owner, inter_error_message **E) {
 	*E = Inode::error(P, I"VERSION_IST structures cannot exist", NULL);
+	return 0;
 }
 
 @ What it does is to specify the version of the Inter language used in the file
@@ -24,7 +25,7 @@ of textual Inter being read in. Now, we can only read a version compatible with
 the version we use ourselves: so we throw an error if it is not.
 
 =
-void VersionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
+int VersionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 	inter_line_parse *ilp, inter_error_location *eloc, inter_error_message **E) {
 	semantic_version_number file_version = VersionNumbers::from_text(ilp->mr.exp[0]);
 	if (InterVersion::check_readable(file_version) == FALSE) {
@@ -35,4 +36,5 @@ void VersionInstruction::read(inter_construct *IC, inter_bookmark *IBM,
 			&file_version, &current_version);
 		*E = InterErrors::plain(erm, eloc);
 	}
+	return 0;
 }
diff --git a/inter/final-module/Chapter 4/Final Inform 6.w b/inter/final-module/Chapter 4/Final Inform 6.w
index 7b36afa80..8889b3840 100644
--- a/inter/final-module/Chapter 4/Final Inform 6.w	
+++ b/inter/final-module/Chapter 4/Final Inform 6.w	
@@ -227,7 +227,7 @@ as any line (including a blank line) doesn't have this signature, I6 exits ICL
 mode.
 
 =
-void I6Target::offer_pragma(code_generator *gtr, code_generation *gen,
+int I6Target::offer_pragma(code_generator *gtr, code_generation *gen,
 	inter_tree_node *P, text_stream *tag, text_stream *content) {
 	if (Str::eq(tag, I"Inform6")) {
 		segmentation_pos saved = CodeGen::select(gen, ICL_directives_I7CGS);
@@ -235,13 +235,15 @@ void I6Target::offer_pragma(code_generator *gtr, code_generation *gen,
 		WRITE("!%% %S\n", content);
 		CodeGen::deselect(gen, saved);
 	}
+	return 0;
 }
 
 @ Names are not mangled: all Inter identifiers are used as-is.
 
 =
-void I6Target::mangle(code_generator *gtr, OUTPUT_STREAM, text_stream *identifier) {
+int I6Target::mangle(code_generator *gtr, OUTPUT_STREAM, text_stream *identifier) {
 	WRITE("%S", identifier);
+	return 0;
 }
 
 @ The end:
diff --git a/inter/final-module/Chapter 4/Inform 6 Code.w b/inter/final-module/Chapter 4/Inform 6 Code.w
index 3913c803b..57ab15da2 100644
--- a/inter/final-module/Chapter 4/Inform 6 Code.w	
+++ b/inter/final-module/Chapter 4/Inform 6 Code.w	
@@ -26,7 +26,7 @@ from the function; the earliest will be used as call parameters, all subsequent
 ones being initially zero.
 
 =
-void I6TargetCode::declare_function(code_generator *gtr, code_generation *gen,
+int I6TargetCode::declare_function(code_generator *gtr, code_generation *gen,
 	vanilla_function *vf) {
 	segmentation_pos saved = CodeGen::select(gen, functions_I7CGS);
 	if (vf == NULL) internal_error("no vg");
@@ -44,6 +44,7 @@ void I6TargetCode::declare_function(code_generator *gtr, code_generation *gen,
 
 	@<Close the function@>;
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
 
 @<Open the function@> =
@@ -155,17 +156,19 @@ A full stop indicates where they are positioned:
 Inter identifiers for labels also start with full stops. So:
 
 =
-void I6TargetCode::place_label(code_generator *gtr, code_generation *gen,
+int I6TargetCode::place_label(code_generator *gtr, code_generation *gen,
 	text_stream *label_name) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("%S;\n", label_name);
+	return 0;
 }
-void I6TargetCode::evaluate_label(code_generator *gtr, code_generation *gen,
+int I6TargetCode::evaluate_label(code_generator *gtr, code_generation *gen,
 	text_stream *label_name) {
 	text_stream *OUT = CodeGen::current(gen);
 	LOOP_THROUGH_TEXT(pos, label_name)
 		if (Str::get(pos) != '.')
 			PUT(Str::get(pos));
+	return 0;
 }
 
 @h Function invocations.
@@ -173,7 +176,7 @@ Or in other words, function calls. These are easy: the syntax is exactly what
 it would be for C.
 
 =
-void I6TargetCode::invoke_function(code_generator *gtr, code_generation *gen,
+int I6TargetCode::invoke_function(code_generator *gtr, code_generation *gen,
 	inter_tree_node *P, vanilla_function *vf, int void_context) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("%S(", vf->identifier);
@@ -184,6 +187,7 @@ void I6TargetCode::invoke_function(code_generator *gtr, code_generation *gen,
 	}
 	WRITE(")");
 	if (void_context) WRITE(";\n");
+	return 0;
 }
 
 @h Assembly language.
@@ -200,7 +204,7 @@ unless we want to save the result. (See the Z-Machine Standards Document.)
 As a dodge, we use the Inform 6 statement |read X Y| instead.
 
 =
-void I6TargetCode::invoke_opcode(code_generator *gtr, code_generation *gen,
+int I6TargetCode::invoke_opcode(code_generator *gtr, code_generation *gen,
 	text_stream *opcode, int operand_count, inter_tree_node **operands,
 	inter_tree_node *label, int label_sense) {
 	text_stream *OUT = CodeGen::current(gen);
@@ -216,9 +220,10 @@ void I6TargetCode::invoke_opcode(code_generator *gtr, code_generation *gen,
 		Vanilla::node(gen, label);
 	}
 	WRITE(";\n");
+	return 0;
 }
 
-void I6TargetCode::assembly_marker(code_generator *gtr, code_generation *gen, inter_ti marker) {
+int I6TargetCode::assembly_marker(code_generator *gtr, code_generation *gen, inter_ti marker) {
 	text_stream *OUT = CodeGen::current(gen);
 	switch (marker) {
 		case ASM_ARROW_ASMMARKER: WRITE("->"); break;
@@ -232,12 +237,13 @@ void I6TargetCode::assembly_marker(code_generator *gtr, code_generation *gen, in
 			WRITE_TO(STDERR, "Unimplemented assembly marker is '%d'\n", marker);
 			internal_error("unimplemented assembly marker");
 	}
+	return 0;
 }
 
 @h Primitives.
 
 =
-void I6TargetCode::invoke_primitive(code_generator *gtr, code_generation *gen,
+int I6TargetCode::invoke_primitive(code_generator *gtr, code_generation *gen,
 	inter_symbol *prim_name, inter_tree_node *P, int void_context) {
 	inter_tree *I = gen->from;
 	text_stream *OUT = CodeGen::current(gen);
@@ -260,6 +266,7 @@ void I6TargetCode::invoke_primitive(code_generator *gtr, code_generation *gen,
 			internal_error("unimplemented prim");
 	}
 	if (suppress_terminal_semicolon == FALSE) WRITE(";\n");
+	return 0;
 }
 
 @ Mostly easy, because the Inter primitives here were so closely modelled on
diff --git a/inter/final-module/Chapter 4/Inform 6 Constants.w b/inter/final-module/Chapter 4/Inform 6 Constants.w
index 72e0fa0ca..f07823307 100644
--- a/inter/final-module/Chapter 4/Inform 6 Constants.w	
+++ b/inter/final-module/Chapter 4/Inform 6 Constants.w	
@@ -22,7 +22,7 @@ on that for |Y| then the declaration for |Y| will always be placed earlier in th
 code than that for |X|. See //Code Generation// for how this is done.
 
 =
-void I6TargetConstants::declare_constant(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::declare_constant(code_generator *gtr, code_generation *gen,
 	inter_symbol *const_s, int form, text_stream *val) {
 	text_stream *const_name = InterSymbol::trans(const_s);
     @<Leave undeclared any array used as a value of a property@>;
@@ -44,6 +44,7 @@ void I6TargetConstants::declare_constant(code_generator *gtr, code_generation *g
 		WRITE(";\n");
 	if (ifndef_me) WRITE("#endif;\n");
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
 
 @ A curious feature of Inform, going back to the original 1970s design of the
@@ -56,7 +57,7 @@ does not need to declare the name of this small array, and so --
 
 @<Leave undeclared any array used as a value of a property@> =
 	if (ConstantInstruction::is_inline(const_s))
-		return;
+		return 0;
 
 @ We cannot declare these constants because they exist automatically, and
 declaring them will therefore throw an error message calling them duplicates.
@@ -74,7 +75,7 @@ anyway.
 		(Str::eq(const_name, I"nothing")) ||
 		(Str::eq(const_name, I"#dict_par1")) ||
 		(Str::eq(const_name, I"#dict_par2")))
-		return;
+		return 0;
 
 @ In addition to those are constants which are only sometimes auto-declared,
 depending on, for example, the choice of virtual machine I6 is compiling to.
@@ -101,7 +102,7 @@ declared with the |Release| directive, so:
 	WRITE("Release ");
 	VanillaConstants::definition_value(gen, form, const_s, val);
 	WRITE(";\n");
-	return;
+	return 0;
 
 @ Likewise the Serial code (e.g., |"211010"|), which must be a double-quoted
 literal:
@@ -110,7 +111,7 @@ literal:
 	WRITE("Serial ");
 	VanillaConstants::definition_value(gen, form, const_s, val);
 	WRITE(";\n");
-	return;
+	return 0;
 
 @h Arrays.
 Now for arrays, which we turn into |Verb| or |Array| directives in I6. We will
@@ -250,18 +251,20 @@ We avoid this by placing semicolons after every entry, just in case:
 Again, though undocumented in the DM4, this is a long-established syntax in I6.
 
 =
-void I6TargetConstants::array_entry(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::array_entry(code_generator *gtr, code_generation *gen,
 	text_stream *entry, int format) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE(" %S;", entry);
+	return 0;
 }
 
-void I6TargetConstants::end_array(code_generator *gtr, code_generation *gen, int format,
+int I6TargetConstants::end_array(code_generator *gtr, code_generation *gen, int format,
 	int zero_count, segmentation_pos *saved) {
 	text_stream *OUT = CodeGen::current(gen);
 	if (zero_count < 0) WRITE(" ];\n");
 	else WRITE("; ! blank with extent %d\n", zero_count);
 	if (saved) CodeGen::deselect(gen, *saved);
+	return 0;
 }
 
 @h Actions.
@@ -273,7 +276,7 @@ in principle but which never occur in any command grammar. It follows that these
 must be explicitly declared, which we do with the I6 |Fake_Action| directive:
 
 =
-void I6TargetConstants::new_action(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::new_action(code_generator *gtr, code_generation *gen,
 	text_stream *name, int true_action, int N) {
 	if (true_action == FALSE) {
 		segmentation_pos saved = CodeGen::select(gen, fake_actions_I7CGS);
@@ -281,6 +284,7 @@ void I6TargetConstants::new_action(code_generator *gtr, code_generation *gen,
 		WRITE("Fake_Action %S;\n", name);
 		CodeGen::deselect(gen, saved);
 	}
+	return 0;
 }
 
 @h Literals.
@@ -290,17 +294,19 @@ a real-number notation including a decimal point. The same notation is used
 by Inter, so we need do nothing to modify it here.
 
 =
-void I6TargetConstants::compile_literal_number(code_generator *gtr,
+int I6TargetConstants::compile_literal_number(code_generator *gtr,
 	code_generation *gen, inter_ti val, int hex_mode) {
 	text_stream *OUT = CodeGen::current(gen);
 	if (hex_mode) WRITE("$%x", val);
 	else WRITE("%d", val);
+	return 0;
 }
 
-void I6TargetConstants::compile_literal_real(code_generator *gtr,
+int I6TargetConstants::compile_literal_real(code_generator *gtr,
 	code_generation *gen, text_stream *textual) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("$%S", textual);
+	return 0;
 }
 
 @ Dictionary words -- those used in the command parser grammar -- are written in
@@ -311,7 +317,7 @@ there, but distinguishes this from the character constant |'z'|, which evaluates
 to the ZSCII code for lower-case Z.
 
 =
-void I6TargetConstants::compile_dictionary_word(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::compile_dictionary_word(code_generator *gtr, code_generation *gen,
 	text_stream *S, int pluralise) {
 	text_stream *OUT = CodeGen::current(gen);
 	int n = 0;
@@ -334,6 +340,7 @@ void I6TargetConstants::compile_dictionary_word(code_generator *gtr, code_genera
 	if (pluralise) WRITE("//p");
 	else if (Str::len(S) == 1) WRITE("//");
 	WRITE("'");
+	return 0;
 }
 
 @ Literal texts, written in double quotation marks, are more of a slog to get
@@ -349,7 +356,7 @@ If you think it surprising that the escape characters would need to use
 different syntaxes in these three cases, I won't argue.
 
 =
-void I6TargetConstants::compile_literal_text(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::compile_literal_text(code_generator *gtr, code_generation *gen,
 	text_stream *S, int escape_mode) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("\"");
@@ -363,6 +370,7 @@ void I6TargetConstants::compile_literal_text(code_generator *gtr, code_generatio
 		@<Compile literal text in value mode@>;
 	}
 	WRITE("\"");
+	return 0;
 }
 
 @ Box mode and value mode are the same except for their handling of newlines.
@@ -443,8 +451,9 @@ hexadecimal.
 @ Finally, names of constants are compiled just as themselves:
 
 =
-void I6TargetConstants::compile_literal_symbol(code_generator *gtr, code_generation *gen,
+int I6TargetConstants::compile_literal_symbol(code_generator *gtr, code_generation *gen,
 	inter_symbol *A) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("%S", InterSymbol::trans(A));
+	return 0;
 }
diff --git a/inter/final-module/Chapter 4/Inform 6 Global Variables.w b/inter/final-module/Chapter 4/Inform 6 Global Variables.w
index 1ab58c10a..627b03f40 100644
--- a/inter/final-module/Chapter 4/Inform 6 Global Variables.w	
+++ b/inter/final-module/Chapter 4/Inform 6 Global Variables.w	
@@ -16,7 +16,7 @@ and have all of the rest use entries in an array called |Global_Vars|, at the co
 of a lookup whenever we read or write them.
 
 =
-void I6TargetVariables::declare_variables(code_generator *gtr, code_generation *gen,
+int I6TargetVariables::declare_variables(code_generator *gtr, code_generation *gen,
 	linked_list *L) {
 	int k = 1;
 	inter_symbol *var_name;
@@ -31,6 +31,7 @@ void I6TargetVariables::declare_variables(code_generator *gtr, code_generation *
 		}
 	}
 	if (k > 1) @<End the array@>;
+	return 0;
 }
 
 @<Begin the array@> =
@@ -68,10 +69,11 @@ void I6TargetVariables::declare_variables(code_generator *gtr, code_generation *
 the variable (lvalue in the case of |as_reference| being set).
 
 =
-void I6TargetVariables::evaluate_variable(code_generator *gtr, code_generation *gen,
+int I6TargetVariables::evaluate_variable(code_generator *gtr, code_generation *gen,
 	inter_symbol *var_name, int as_reference) {
 	text_stream *OUT = CodeGen::current(gen);
 	int k = SymbolAnnotation::get_i(var_name, I6_GLOBAL_OFFSET_IANN);
 	if (k > 0) WRITE("(Global_Vars-->%d)", k);
 	else WRITE("%S", InterSymbol::trans(var_name));
+	return 0;
 }
diff --git a/inter/final-module/Chapter 4/Inform 6 Objects.w b/inter/final-module/Chapter 4/Inform 6 Objects.w
index 04579d3da..a54f0462f 100644
--- a/inter/final-module/Chapter 4/Inform 6 Objects.w	
+++ b/inter/final-module/Chapter 4/Inform 6 Objects.w	
@@ -71,7 +71,7 @@ VM-property number or a VM-attribute number. Indeed, |potency| might compile
 to the same number as |edible|. So word 0 is certainly necessary.
 
 =
-void I6TargetObjects::declare_property(code_generator *gtr, code_generation *gen,
+int I6TargetObjects::declare_property(code_generator *gtr, code_generation *gen,
 	inter_symbol *prop_s, linked_list *all_forms) {
 	text_stream *inner_name = VanillaObjects::inner_property_name(gen, prop_s);
 
@@ -86,6 +86,7 @@ void I6TargetObjects::declare_property(code_generator *gtr, code_generation *gen
 	else                          @<Store this in an undeclared VM-property@>;
 
 	@<Compile the two opening words of the property metadata@>;
+	return 0;
 }
 
 @ For why there are multiple declarations of the same property in the Inter
@@ -240,13 +241,14 @@ The following is called for all kinds which can have properties. We divide
 them in two:
 
 =
-void I6TargetObjects::declare_kind(code_generator *gtr, code_generation *gen, 
+int I6TargetObjects::declare_kind(code_generator *gtr, code_generation *gen, 
 	inter_symbol *kind_s, segmentation_pos *saved) {
 	if ((kind_s == RunningPipelines::get_symbol(gen->from_step, object_kind_RPSYM)) ||
 		(VanillaObjects::is_kind_of_object(gen, kind_s)))
 		@<A kind of object, including the kind object itself@>
 	else if (VanillaObjects::value_kind_with_properties(gen, kind_s))
 		@<A property-holding enumeration kind@>;
+	return 0;
 }
 
 @ Each object kind is compiled to a VM-class:
@@ -275,7 +277,7 @@ to the VPH VM-object.
 	DISCARD_TEXT(instance_name)
 
 @ =
-void I6TargetObjects::end_kind(code_generator *gtr, code_generation *gen,
+int I6TargetObjects::end_kind(code_generator *gtr, code_generation *gen,
 	inter_symbol *kind_s, segmentation_pos saved) {
 	if ((kind_s == RunningPipelines::get_symbol(gen->from_step, object_kind_RPSYM)) ||
 		(VanillaObjects::is_kind_of_object(gen, kind_s))) {
@@ -285,12 +287,13 @@ void I6TargetObjects::end_kind(code_generator *gtr, code_generation *gen,
 	} else if (VanillaObjects::value_kind_with_properties(gen, kind_s)) {
 		I6TargetObjects::VM_object_footer(gen, saved);
 	}
+	return 0;
 }
 
 @ Instances next:
 
 =
-void I6TargetObjects::declare_instance(code_generator *gtr,
+int I6TargetObjects::declare_instance(code_generator *gtr,
 	code_generation *gen, inter_symbol *inst_s, inter_symbol *kind_s, int enumeration,
 	segmentation_pos *saved) {
 	if ((kind_s == RunningPipelines::get_symbol(gen->from_step, object_kind_RPSYM)) ||
@@ -298,6 +301,7 @@ void I6TargetObjects::declare_instance(code_generator *gtr,
 		@<An object instance@>
 	else
 		@<A value instance@>;
+	return 0;
 }
 
 @ Each instance of a kind of object becomes a VM-object:
@@ -326,11 +330,12 @@ enumerated these colours as 1, 2, 3.
 	DISCARD_TEXT(val)
 
 @ =
-void I6TargetObjects::end_instance(code_generator *gtr, code_generation *gen,
+int I6TargetObjects::end_instance(code_generator *gtr, code_generation *gen,
 	inter_symbol *inst_s, inter_symbol *kind_s, segmentation_pos saved) {
 	if ((kind_s == RunningPipelines::get_symbol(gen->from_step, object_kind_RPSYM)) ||
 		(VanillaObjects::is_kind_of_object(gen, kind_s)))
 		I6TargetObjects::VM_object_footer(gen, saved);
+	return 0;
 }
 
 @ For the I6 header syntax, see the DM4. Note that the "hardwired" short
@@ -382,12 +387,13 @@ other one used by the standard kits supplied with Inform is |thedark|. I urge
 people to create no further pseudo-objects.
 
 =
-void I6TargetObjects::pseudo_object(code_generator *gtr, code_generation *gen, text_stream *obj_name) {
+int I6TargetObjects::pseudo_object(code_generator *gtr, code_generation *gen, text_stream *obj_name) {
 	segmentation_pos saved;
 	I6TargetObjects::VM_object_header(gen, I"Object", obj_name, NULL, 0, FALSE, &saved);
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE(" \"(%S object)\"\n    has concealed\n", obj_name);
 	I6TargetObjects::VM_object_footer(gen, saved);
+	return 0;
 }
 
 @ That just leaves property values. The wrinkle here is the peculiar syntax
@@ -403,7 +409,7 @@ address of the address of the array, not the address of the array itself. So
 we must use the peculiar I6 syntax here to get the right outcome.
 
 =
-void I6TargetObjects::assign_property(code_generator *gtr, code_generation *gen,
+int I6TargetObjects::assign_property(code_generator *gtr, code_generation *gen,
 	inter_symbol *prop_s, inter_pair pair, inter_tree_node *X) {
 	TEMPORARY_TEXT(val)
 	CodeGen::select_temporary(gen, val);
@@ -425,15 +431,17 @@ void I6TargetObjects::assign_property(code_generator *gtr, code_generation *gen,
 	CodeGen::deselect_temporary(gen);
 	I6TargetObjects::VM_property(gen, prop_s, val);
 	DISCARD_TEXT(val)
+	return 0;
 }
 
 @ And this much easier function assigns a stick of property values for a property
 of a value kind. (An array which is not inline.)
 
 =
-void I6TargetObjects::assign_properties(code_generator *gtr, code_generation *gen,
+int I6TargetObjects::assign_properties(code_generator *gtr, code_generation *gen,
 	inter_symbol *kind_name, inter_symbol *prop_s, text_stream *array) {
 	I6TargetObjects::VM_property(gen, prop_s, array);
+	return 0;
 }
 
 @h A few resources.
diff --git a/inter/final-module/Chapter 5/C Assembly.w b/inter/final-module/Chapter 5/C Assembly.w
index 11f5eb07b..813cde264 100644
--- a/inter/final-module/Chapter 5/C Assembly.w	
+++ b/inter/final-module/Chapter 5/C Assembly.w	
@@ -241,7 +241,7 @@ predeclare this:
 instruction found in our Inter.
 
 =
-void CAssembly::invoke_opcode(code_generator *gtr, code_generation *gen,
+int CAssembly::invoke_opcode(code_generator *gtr, code_generation *gen,
 	text_stream *opcode, int operand_count, inter_tree_node **operands,
 	inter_tree_node *label, int label_sense) {
 	C_supported_opcode *opc = CAssembly::find_opcode(gen, opcode, operand_count);
@@ -253,6 +253,7 @@ void CAssembly::invoke_opcode(code_generator *gtr, code_generation *gen,
 	if (label_sense != NOT_APPLICABLE) @<End a branch instruction@>;
 	@<Push any stored results which need to end up on the stack@>;
 	WRITE(";\n");
+	return 0;
 }
 
 @<Begin a branch instruction@> =
@@ -340,7 +341,7 @@ by attempting to write |0| in a pointer context, so we pass it as |NULL| instead
 @ And where does the special operand |sp| come from? From here:
 
 =
-void CAssembly::assembly_marker(code_generator *gtr, code_generation *gen, inter_ti marker) {
+int CAssembly::assembly_marker(code_generator *gtr, code_generation *gen, inter_ti marker) {
 	text_stream *OUT = CodeGen::current(gen);
 	switch (marker) {
 		case ASM_SP_ASMMARKER: WRITE("sp"); break;
@@ -348,6 +349,7 @@ void CAssembly::assembly_marker(code_generator *gtr, code_generation *gen, inter
 			WRITE_TO(STDERR, "Unsupported assembly marker is '%d'\n", marker);
 			internal_error("unsupported assembly marker in C");
 	}
+	return 0;
 }
 
 @h call.
diff --git a/inter/final-module/Chapter 5/C Function Model.w b/inter/final-module/Chapter 5/C Function Model.w
index 3af62e716..edd13d5b3 100644
--- a/inter/final-module/Chapter 5/C Function Model.w	
+++ b/inter/final-module/Chapter 5/C Function Model.w	
@@ -38,7 +38,7 @@ following places a predeclaration of that function high up in our C code, so
 that we then don't need to worry about code-ordering when compiling calls to it:
 
 =
-void CFunctionModel::predeclare_function(code_generator *gtr, code_generation *gen,
+int CFunctionModel::predeclare_function(code_generator *gtr, code_generation *gen,
 	vanilla_function *vf) {
 	segmentation_pos saved = CodeGen::select(gen, c_predeclarations_I7CGS);
 	text_stream *OUT = CodeGen::current(gen);
@@ -46,6 +46,7 @@ void CFunctionModel::predeclare_function(code_generator *gtr, code_generation *g
 	WRITE(";\n");
 	CodeGen::deselect(gen, saved);
 	CFunctionModel::declare_function_constant(gen, vf);
+	return 0;
 }
 
 @ And this expresses what the C prototype for that function will be. For example,
@@ -131,7 +132,7 @@ First, the straightforward and most common case: calling a function whose identi
 is known at run-time, and is passed to us as |vf|.
 
 =
-void CFunctionModel::invoke_function(code_generator *gtr, code_generation *gen,
+int CFunctionModel::invoke_function(code_generator *gtr, code_generation *gen,
 	inter_tree_node *P, vanilla_function *vf, int void_context) {
 	text_stream *OUT = CodeGen::current(gen);
 	CFunctionModel::C_function_identifier(gen, OUT, vf);
@@ -140,6 +141,7 @@ void CFunctionModel::invoke_function(code_generator *gtr, code_generation *gen,
 	else @<Supply arguments as call parameters@>;
 	WRITE(")");
 	if (void_context) WRITE(";\n");
+	return 0;
 }
 
 @ As noted above, all unsupplied call parameters are filled in with zeroes:
@@ -305,7 +307,7 @@ void CFunctionModel::unsafe_C_function_prototype(code_generation *gen, OUTPUT_ST
 }
 
 @ =
-void CFunctionModel::declare_function(code_generator *gtr, code_generation *gen,
+int CFunctionModel::declare_function(code_generator *gtr, code_generation *gen,
 	vanilla_function *vf) {
 	text_stream *fn_name = vf->identifier;
 	segmentation_pos saved = CodeGen::select(gen, c_function_declarations_I7CGS);
@@ -313,6 +315,7 @@ void CFunctionModel::declare_function(code_generator *gtr, code_generation *gen,
 	@<Compile the functional part@>;
 	if (vf->takes_variable_arguments == FALSE) @<Compile the stack-safe outer function@>;
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
 
 @<Compile the functional part@> =
@@ -366,13 +369,14 @@ And in case that is what we need here, we always place an empty statement after
 a label.
 
 =
-void CFunctionModel::place_label(code_generator *gtr, code_generation *gen,
+int CFunctionModel::place_label(code_generator *gtr, code_generation *gen,
 	text_stream *label_name) {
 	text_stream *OUT = CodeGen::current(gen);
 	LOOP_THROUGH_TEXT(pos, label_name)
 		if (Str::get(pos) != '.')
 			PUT(Str::get(pos));
 	WRITE(": ;\n", label_name);
+	return 0;
 }
 
 @ Labels are not really "evaluated" in C: |goto| destinations are not values.
@@ -384,12 +388,13 @@ occur, are unmangled. This is safe because label names have their own namespace
 in C, so they cannot clash with other identifiers.
 
 =
-void CFunctionModel::evaluate_label(code_generator *gtr, code_generation *gen,
+int CFunctionModel::evaluate_label(code_generator *gtr, code_generation *gen,
 	text_stream *label_name) {
 	text_stream *OUT = CodeGen::current(gen);
 	LOOP_THROUGH_TEXT(pos, label_name)
 		if (Str::get(pos) != '.')
 			PUT(Str::get(pos));
+	return 0;
 }
 
 @h Outward-bound function calls.
diff --git a/inter/final-module/Chapter 5/C Global Variables.w b/inter/final-module/Chapter 5/C Global Variables.w
index bf93c85d2..802173c61 100644
--- a/inter/final-module/Chapter 5/C Global Variables.w	
+++ b/inter/final-module/Chapter 5/C Global Variables.w	
@@ -41,7 +41,7 @@ the special variable |self| given index 0. Note that |self| always exists,
 but has no Inter declaration node.
 
 =
-void CGlobals::declare_variables(code_generator *gtr, code_generation *gen, linked_list *L) {
+int CGlobals::declare_variables(code_generator *gtr, code_generation *gen, linked_list *L) {
 	segmentation_pos saved = CodeGen::select(gen, c_globals_array_I7CGS);
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("i7word_t i7_initial_variable_values[i7_no_variables] = {\n");
@@ -63,6 +63,7 @@ void CGlobals::declare_variables(code_generator *gtr, code_generation *gen, link
 
 	WRITE("};\n");
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
 
 @<Define a constant for the self position in the globals array@> =
@@ -123,12 +124,13 @@ void CGlobals::define_header_constant_for_variable(code_generation *gen, text_st
 @ Within a process |proc|, the current value of variable |i| is |proc->state.variables[i]|.
 
 =
-void CGlobals::evaluate_variable(code_generator *gtr, code_generation *gen,
+int CGlobals::evaluate_variable(code_generator *gtr, code_generation *gen,
 	inter_symbol *var_name, int as_reference) {
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("proc->state.variables[");
 	CNamespace::mangle_variable(OUT, InterSymbol::trans(var_name));
 	WRITE("]");
+	return 0;
 }
 
 @ Finally, this function, part of the C library, initialises the variables for a
diff --git a/inter/final-module/Chapter 5/C Literals.w b/inter/final-module/Chapter 5/C Literals.w
index 3fabdd942..a097b6b62 100644
--- a/inter/final-module/Chapter 5/C Literals.w	
+++ b/inter/final-module/Chapter 5/C Literals.w	
@@ -42,11 +42,12 @@ just by naming it. That seems too obvious to need a function, but one can imagin
 languages where it is not true.
 
 =
-void CLiteralsModel::compile_literal_symbol(code_generator *gtr, code_generation *gen,
+int CLiteralsModel::compile_literal_symbol(code_generator *gtr, code_generation *gen,
 	inter_symbol *aliased) {
 	text_stream *OUT = CodeGen::current(gen);
 	text_stream *S = InterSymbol::trans(aliased);
 	Generators::mangle(gen, OUT, S);
+	return 0;
 }
 
 @h Integers.
@@ -55,11 +56,12 @@ and show the number in hex in the code it generates; functionally, decimal would
 be just as good. But since we can easily do so, why not.
 
 =
-void CLiteralsModel::compile_literal_number(code_generator *gtr,
+int CLiteralsModel::compile_literal_number(code_generator *gtr,
 	code_generation *gen, inter_ti val, int hex_mode) {
 	text_stream *OUT = CodeGen::current(gen);
 	if (hex_mode) WRITE("0x%x", val);
 	else WRITE("%d", val);
+	return 0;
 }
 
 @h Real numbers.
@@ -67,11 +69,12 @@ This is not at all simple, but the helpful //VanillaConstants::textual_real_to_u
 does all the work for us.
 
 =
-void CLiteralsModel::compile_literal_real(code_generator *gtr,
+int CLiteralsModel::compile_literal_real(code_generator *gtr,
 	code_generation *gen, text_stream *textual) {
 	uint32_t n = VanillaConstants::textual_real_to_uint32(textual);
 	text_stream *OUT = CodeGen::current(gen);
 	WRITE("(i7word_t) 0x%08x", n);
+	return 0;
 }
 
 @h Texts.
@@ -110,7 +113,7 @@ started here:
 void CLiteralsModel::begin_text(code_generation *gen) {
 }
 
-void CLiteralsModel::compile_literal_text(code_generator *gtr, code_generation *gen,
+int CLiteralsModel::compile_literal_text(code_generator *gtr, code_generation *gen,
 	text_stream *S, int no_special_characters) {
 	text_stream *OUT = CodeGen::current(gen);
 	if (gen->literal_text_mode == REGULAR_LTM) {
@@ -121,6 +124,7 @@ void CLiteralsModel::compile_literal_text(code_generator *gtr, code_generation *
 	} else {
 		@<Compile the text@>;
 	}
+	return 0;
 }
 
 @<Compile the text@> =
@@ -217,7 +221,7 @@ True actions count upwards from 0; fake actions independently count upwards
 from 4096. These are defined just as constants, with mangled names:
 
 =
-void CLiteralsModel::new_action(code_generator *gtr, code_generation *gen,
+int CLiteralsModel::new_action(code_generator *gtr, code_generation *gen,
 	text_stream *name, int true_action, int N) {
 	if (true_action) {
 		segmentation_pos saved = CodeGen::select(gen, c_actions_symbols_I7CGS);
@@ -237,6 +241,7 @@ void CLiteralsModel::new_action(code_generator *gtr, code_generation *gen,
 
 	DISCARD_TEXT(O)
 	DISCARD_TEXT(M)
+	return 0;
 }
 
 @h Dictionary words.
@@ -275,9 +280,10 @@ void i7_print_dword(i7process_t *proc, i7word_t at) {
 so there is very little to do:
 
 =
-void CLiteralsModel::compile_dictionary_word(code_generator *gtr, code_generation *gen,
+int CLiteralsModel::compile_dictionary_word(code_generator *gtr, code_generation *gen,
 	text_stream *S, int pluralise) {
 	text_stream *OUT = CodeGen::current(gen);
 	vanilla_dword *dw = VanillaIF::text_to_noun_dword(gen, S, pluralise);
 	CNamespace::mangle(gtr, OUT, dw->identifier);
+	return 0;
 }
diff --git a/inter/final-module/Chapter 5/C Memory Model.w b/inter/final-module/Chapter 5/C Memory Model.w
index 96afa54eb..5aca5bfa4 100644
--- a/inter/final-module/Chapter 5/C Memory Model.w	
+++ b/inter/final-module/Chapter 5/C Memory Model.w	
@@ -150,7 +150,7 @@ which we will retrospectively predefine when the array ends.
 @ The call to //CMemoryModel::begin_array// is then followed by a series of calls to:
 
 =
-void CMemoryModel::array_entry(code_generator *gtr, code_generation *gen,
+int CMemoryModel::array_entry(code_generator *gtr, code_generation *gen,
 	text_stream *entry, int format) {
 	segmentation_pos saved = CodeGen::select(gen, c_memory_array_I7CGS);
 	text_stream *OUT = CodeGen::current(gen);
@@ -160,6 +160,7 @@ void CMemoryModel::array_entry(code_generator *gtr, code_generation *gen,
 		@<This is a byte entry@>;
 	CodeGen::deselect(gen, saved);
 	C_GEN_DATA(memdata.entry_count)++;
+	return 0;
 }
 
 @<This is a byte entry@> =
@@ -180,7 +181,7 @@ and therefore if |X| is a valid constant-context expression in C then so is
 except to predeclare the extent constant.
 
 =
-void CMemoryModel::end_array(code_generator *gtr, code_generation *gen, int format,
+int CMemoryModel::end_array(code_generator *gtr, code_generation *gen, int format,
 	int zero_count, segmentation_pos *saved) {
 	segmentation_pos x_saved = CodeGen::select(gen, c_predeclarations_I7CGS);
 	text_stream *OUT = CodeGen::current(gen);
@@ -189,6 +190,7 @@ void CMemoryModel::end_array(code_generator *gtr, code_generation *gen, int form
 	WRITE("__xt %d\n", C_GEN_DATA(memdata.entry_count)-1);
 	CodeGen::deselect(gen, x_saved);
 	if (saved) CodeGen::deselect(gen, *saved);
+	return 0;
 }
 
 @ The primitives for byte and word lookup have the signatures:
@@ -361,9 +363,10 @@ void i7_write_word(i7process_t *proc, i7word_t address, i7word_t array_index,
 =
 
 @ =
-void CMemoryModel::word_to_byte(code_generator *gtr, code_generation *gen,
+int CMemoryModel::word_to_byte(code_generator *gtr, code_generation *gen,
 	OUTPUT_STREAM, text_stream *val, int b) {
 	WRITE("I7BYTE_%d(%S)", b, val);
+	return 0;
 }
 
 @ The seven primitive operations on storage need to be implemented for byte
diff --git a/inter/final-module/Chapter 5/C Namespace.w b/inter/final-module/Chapter 5/C Namespace.w
index f01ad6136..45fbc3e7b 100644
--- a/inter/final-module/Chapter 5/C Namespace.w	
+++ b/inter/final-module/Chapter 5/C Namespace.w	
@@ -32,13 +32,14 @@ to the same result, |i7_ss_aa|. But |#| and |$| characters are used extremely
 sparingly in Inter, and these can never arise.
 
 =
-void CNamespace::mangle(code_generator *gtr, OUTPUT_STREAM, text_stream *identifier) {
+int CNamespace::mangle(code_generator *gtr, OUTPUT_STREAM, text_stream *identifier) {
 	if (Str::get_first_char(identifier) == '#') {
 		WRITE("i7_ss_");
 		LOOP_THROUGH_TEXT(pos, identifier)
 			if ((Str::get(pos) != '#') && (Str::get(pos) != '$'))
 				PUT(Str::get(pos));
 	} else WRITE("i7_mgl_%S", identifier);
+	return 0;
 }
 
 void CNamespace::mangle_with(code_generator *gtr, OUTPUT_STREAM, text_stream *identifier,
@@ -127,7 +128,7 @@ For the reason why |Serial| and |Release| are placed higher-up in the file, see
 //C Memory Model//.
 
 =
-void CNamespace::declare_constant(code_generator *gtr, code_generation *gen,
+int CNamespace::declare_constant(code_generator *gtr, code_generation *gen,
 	inter_symbol *const_s, int form, text_stream *val) {
 	text_stream *name = InterSymbol::trans(const_s);
 	int seg = c_constants_I7CGS;
@@ -143,4 +144,5 @@ void CNamespace::declare_constant(code_generator *gtr, code_generation *gen,
 	VanillaConstants::definition_value(gen, form, const_s, val);
 	WRITE("\n");
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
diff --git a/inter/final-module/Chapter 5/C Object Model.w b/inter/final-module/Chapter 5/C Object Model.w
index 9339b2cf7..2d182f8b7 100644
--- a/inter/final-module/Chapter 5/C Object Model.w	
+++ b/inter/final-module/Chapter 5/C Object Model.w	
@@ -299,13 +299,14 @@ void CObjectModel::declare_metaclasses(code_generation *gen) {
 @ And here is (ii):
 
 =
-void CObjectModel::declare_kind(code_generator *gtr, code_generation *gen, 
+int CObjectModel::declare_kind(code_generator *gtr, code_generation *gen, 
 	inter_symbol *kind_s, segmentation_pos *saved) {
 	if ((kind_s == RunningPipelines::get_symbol(gen->from_step, object_kind_RPSYM)) ||
 		(VanillaObjects::is_kind_of_object(gen, kind_s)))
 		@<Declare a kind of object@>
 	else if (VanillaObjects::value_kind_with_properties(gen, kind_s))
 		CObjectModel::vph_object(gen, kind_s);
+	return 0;
 }
 
 @<Declare a kind of object@> =
@@ -349,9 +350,10 @@ Here is (i). After a typical IF run through Inform 7, this produces only
 two pseudo-objects, |Compass| and |thedark|.
 
 =
-void CObjectModel::pseudo_object(code_generator *gtr, code_generation *gen, text_stream *obj_name) {
+int CObjectModel::pseudo_object(code_generator *gtr, code_generation *gen, text_stream *obj_name) {
 	C_property_owner *obj = CObjectModel::new_runtime_object(gtr, gen, I"Object", obj_name, -1, FALSE);
 	if (Str::eq(obj_name, I"Compass")) C_GEN_DATA(objdata.compass_instance) = obj;
+	return 0;
 }
 
 @ Here is (ii). Each enumerated kind produces one of these. In a typical
@@ -368,7 +370,7 @@ void CObjectModel::vph_object(code_generation *gen, inter_symbol *kind_s) {
 @ And here is (iii).
 
 =
-void CObjectModel::declare_instance(code_generator *gtr, code_generation *gen,
+int CObjectModel::declare_instance(code_generator *gtr, code_generation *gen,
 	inter_symbol *inst_s, inter_symbol *kind_s, int enumeration, segmentation_pos *ignored_saved) {
 	text_stream *printed_name = Metadata::optional_textual(
 		InterPackage::container(inst_s->definition), I"^printed_name");
@@ -386,6 +388,7 @@ void CObjectModel::declare_instance(code_generator *gtr, code_generation *gen,
 	WRITE("#define %S %d\n",
 		CTarget::symbols_header_identifier(gen, I"I", printed_name), enumeration);
 	CodeGen::deselect(gen, saved);
+	return 0;
 }
 
 @<Declare an object instance@> =
@@ -483,7 +486,7 @@ C_property *CObjectModel::existing_property_by_name(code_generation *gen,
 @h Declaring properties.
 
 =
-void CObjectModel::declare_property(code_generator *gtr, code_generation *gen,
+int CObjectModel::declare_property(code_generator *gtr, code_generation *gen,
 	inter_symbol *prop_s, linked_list *all_forms) {
 	text_stream *name = InterSymbol::trans(prop_s);
 	int either_or = VanillaObjects::is_either_or_property(prop_s);
@@ -493,6 +496,7 @@ void CObjectModel::declare_property(code_generator *gtr, code_generation *gen,
 	@<Define the inner name as a constant@>;
 	@<Make the opening two metadata array entries required by Vanilla@>;
 	@<Define the property name in the symbols header file too@>;
+	return 0;
 }
 
 @<Define the inner name as a constant@> =
@@ -538,7 +542,7 @@ But the second entry is the inner property, as with Inform 6.
 Vabilla calls this to assign a property to a single owner:
 
 =
-void CObjectModel::assign_property(code_generator *gtr, code_generation *gen,
+int CObjectModel::assign_property(code_generator *gtr, code_generation *gen,
 	inter_symbol *prop_s, inter_pair pair, inter_tree_node *X) {
 
 	int inline_this = FALSE;
@@ -555,13 +559,14 @@ void CObjectModel::assign_property(code_generator *gtr, code_generation *gen,
 		InterSymbol::trans(prop_s));
 	CObjectModel::assign_one_prop(gen, owner, prop, val, inline_this);
 	DISCARD_TEXT(val)
+	return 0;
 }
 
 @ And it calls this to give an array of the property's values for all of the
 instances of a single enumerated kind:
 
 =
-void CObjectModel::assign_properties(code_generator *gtr, code_generation *gen,
+int CObjectModel::assign_properties(code_generator *gtr, code_generation *gen,
 	inter_symbol *kind_s, inter_symbol *prop_s, text_stream *array) {
 	TEMPORARY_TEXT(mgl)
 	Generators::mangle(gen, mgl, array);
@@ -570,6 +575,7 @@ void CObjectModel::assign_properties(code_generator *gtr, code_generation *gen,
 		InterSymbol::trans(prop_s));
 	CObjectModel::assign_one_prop(gen, owner, prop, mgl, FALSE);
 	DISCARD_TEXT(mgl)
+	return 0;
 }
 
 @ In either case, the following assigns a property value to an owner, though
diff --git a/inter/final-module/Chapter 5/C Program Control.w b/inter/final-module/Chapter 5/C Program Control.w
index 8d6659d0e..9f225dd33 100644
--- a/inter/final-module/Chapter 5/C Program Control.w	
+++ b/inter/final-module/Chapter 5/C Program Control.w	
@@ -11,7 +11,7 @@ void CProgramControl::initialise(code_generator *gtr) {
 	METHOD_ADD(gtr, INVOKE_PRIMITIVE_MTID, CProgramControl::invoke_primitive);
 }
 
-void CProgramControl::invoke_primitive(code_generator *gtr, code_generation *gen,
+int CProgramControl::invoke_primitive(code_generator *gtr, code_generation *gen,
 	inter_symbol *prim_name, inter_tree_node *P, int void_context) {
 	inter_tree *I = gen->from;
 	inter_ti bip = Primitives::to_BIP(I, prim_name);
@@ -28,6 +28,7 @@ void CProgramControl::invoke_primitive(code_generator *gtr, code_generation *gen
 		text_stream *OUT = CodeGen::current(gen);
 		WRITE(";\n");
 	}
+	return 0;
 }
 
 @ And the rest of this section implements the primitives to do with execution
diff --git a/services/calculus-module/Chapter 2/Kind Predicates.w b/services/calculus-module/Chapter 2/Kind Predicates.w
index a56fe48f6..2c2f7974e 100644
--- a/services/calculus-module/Chapter 2/Kind Predicates.w	
+++ b/services/calculus-module/Chapter 2/Kind Predicates.w	
@@ -96,8 +96,9 @@ void KindPredicates::set_unarticled(pcalc_prop *prop, int state) {
 	}
 }
 
-void KindPredicates::infer_kind(up_family *self, unary_predicate *up, kind **K) {
+int KindPredicates::infer_kind(up_family *self, unary_predicate *up, kind **K) {
 	*K = up->assert_kind;
+	return 0;
 }
 
 @ The following functions express that (i) |kind=K| predicates can always be
@@ -116,9 +117,10 @@ int KindPredicates::test(up_family *self, unary_predicate *up,
 }
 
 @ =
-void KindPredicates::log_kind(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
+int KindPredicates::log_kind(up_family *self, OUTPUT_STREAM, unary_predicate *up) {
 	if (Streams::I6_escapes_enabled(OUT) == FALSE) WRITE("kind=");
 	WRITE("%u", up->assert_kind);
 	if ((Streams::I6_escapes_enabled(OUT) == FALSE) && (up->composited)) WRITE("_c");
 	if ((Streams::I6_escapes_enabled(OUT) == FALSE) && (up->unarticled)) WRITE("_u");
+	return 0;
 }
diff --git a/services/calculus-module/Chapter 3/The Equality Relation.w b/services/calculus-module/Chapter 3/The Equality Relation.w
index 4d316c1d4..3de60bbb6 100644
--- a/services/calculus-module/Chapter 3/The Equality Relation.w	
+++ b/services/calculus-module/Chapter 3/The Equality Relation.w	
@@ -49,15 +49,16 @@ void Calculus::Equality::start(void) {
 Note the unique one-off way in which equality is made.
 
 =
-void Calculus::Equality::stock(bp_family *self, int n) {
+int Calculus::Equality::stock(bp_family *self, int n) {
 	if (n == 1) {
 		R_equality = BinaryPredicates::make_equality(equality_bp_family,
 			PreformUtilities::wording(<relation-names>, EQUALITY_RELATION_NAME));
 		BinaryPredicates::set_index_details(R_equality, "value", "value");
 	}
+	return 0;
 }
 
-void Calculus::Equality::stock_spatial(bp_family *self, int n) {
+int Calculus::Equality::stock_spatial(bp_family *self, int n) {
 	if (n == 1) {
 		a_has_b_predicate =
 			BinaryPredicates::make_pair(spatial_bp_family,
@@ -67,14 +68,16 @@ void Calculus::Equality::stock_spatial(bp_family *self, int n) {
 				NULL, NULL,
 				PreformUtilities::wording(<relation-names>, POSSESSION_RELATION_NAME));
 	}
+	return 0;
 }
 
-void Calculus::Equality::stock_empty(bp_family *self, int n) {
+int Calculus::Equality::stock_empty(bp_family *self, int n) {
 	if (n == 1) {
 		R_empty = BinaryPredicates::make_equality(empty_bp_family,
 			PreformUtilities::wording(<relation-names>, EMPTY_RELATION_NAME));
 		BinaryPredicates::set_index_details(R_equality, "value", "value");
 	}
+	return 0;
 }
 
 @h Problem message text.
@@ -84,15 +87,17 @@ int Calculus::Equality::describe_for_problems(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	return FALSE;
 }
-void Calculus::Equality::describe_for_index(bp_family *self, OUTPUT_STREAM,
+int Calculus::Equality::describe_for_index(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	WRITE("equality");
+	return 0;
 }
 int Calculus::Equality::describe_empty_for_problems(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	return FALSE;
 }
-void Calculus::Equality::describe_empty_for_index(bp_family *self, OUTPUT_STREAM,
+int Calculus::Equality::describe_empty_for_index(bp_family *self, OUTPUT_STREAM,
 	binary_predicate *bp) {
 	WRITE("never-holding");
+	return 0;
 }
diff --git a/services/linguistics-module/Chapter 2/Adjectives.w b/services/linguistics-module/Chapter 2/Adjectives.w
index 27f438346..efc8778e6 100644
--- a/services/linguistics-module/Chapter 2/Adjectives.w	
+++ b/services/linguistics-module/Chapter 2/Adjectives.w	
@@ -38,9 +38,10 @@ void Adjectives::create_category(void) {
 	METHOD_ADD(adjectives_category, LOG_GRAMMATICAL_CATEGORY_MTID, Adjectives::log_item);
 }
 
-void Adjectives::log_item(grammatical_category *cat, general_pointer data) {
+int Adjectives::log_item(grammatical_category *cat, general_pointer data) {
 	adjective *adj = RETRIEVE_POINTER_adjective(data);
 	Adjectives::log(adj);
+	return 0;
 }
 
 adjective *Adjectives::from_lcon(lcon_ti lcon) {
diff --git a/services/linguistics-module/Chapter 2/Articles.w b/services/linguistics-module/Chapter 2/Articles.w
index 333b22320..21d56e657 100644
--- a/services/linguistics-module/Chapter 2/Articles.w	
+++ b/services/linguistics-module/Chapter 2/Articles.w	
@@ -53,9 +53,10 @@ article *Articles::new(text_stream *name) {
 	return P;
 }
 
-void Articles::log_item(grammatical_category *cat, general_pointer data) {
+int Articles::log_item(grammatical_category *cat, general_pointer data) {
 	article *P = RETRIEVE_POINTER_article(data);
 	LOG("%S", P->name);
+	return 0;
 }
 
 @h Stock references.
diff --git a/services/linguistics-module/Chapter 2/Determiners and Quantifiers.w b/services/linguistics-module/Chapter 2/Determiners and Quantifiers.w
index dc8fb5ef4..17d956a2b 100644
--- a/services/linguistics-module/Chapter 2/Determiners and Quantifiers.w	
+++ b/services/linguistics-module/Chapter 2/Determiners and Quantifiers.w	
@@ -105,9 +105,10 @@ void Quantifiers::create_category(void) {
 	METHOD_ADD(determiners_category, LOG_GRAMMATICAL_CATEGORY_MTID, Quantifiers::log_item);
 }
 
-void Quantifiers::log_item(grammatical_category *cat, general_pointer data) {
+int Quantifiers::log_item(grammatical_category *cat, general_pointer data) {
 	determiner *D = RETRIEVE_POINTER_determiner(data);
 	LOG("'%A' --> %s", &(D->text_of_det), D->quantifier_meant->log_text);
+	return 0;
 }
 
 @h Creating a quantifier.
diff --git a/services/linguistics-module/Chapter 2/Nouns.w b/services/linguistics-module/Chapter 2/Nouns.w
index e0c595028..01ca79755 100644
--- a/services/linguistics-module/Chapter 2/Nouns.w	
+++ b/services/linguistics-module/Chapter 2/Nouns.w	
@@ -94,10 +94,11 @@ void Nouns::create_category(void) {
 	METHOD_ADD(nouns_category, LOG_GRAMMATICAL_CATEGORY_MTID, Nouns::log_item);
 }
 
-void Nouns::log_item(grammatical_category *cat, general_pointer data) {
+int Nouns::log_item(grammatical_category *cat, general_pointer data) {
 	noun *N = RETRIEVE_POINTER_noun(data);
 	if (N->noun_subclass == COMMON_NOUN) LOG("common: "); else LOG("proper: ");
 	Nouns::log(N);
+	return 0;
 }
 
 @h Creation.
diff --git a/services/linguistics-module/Chapter 2/Pronouns.w b/services/linguistics-module/Chapter 2/Pronouns.w
index 9deb94a9c..f4eda895c 100644
--- a/services/linguistics-module/Chapter 2/Pronouns.w	
+++ b/services/linguistics-module/Chapter 2/Pronouns.w	
@@ -74,9 +74,10 @@ pronoun *Pronouns::new(text_stream *name) {
 	return P;
 }
 
-void Pronouns::log_item(grammatical_category *cat, general_pointer data) {
+int Pronouns::log_item(grammatical_category *cat, general_pointer data) {
 	pronoun *P = RETRIEVE_POINTER_pronoun(data);
 	LOG("%S", P->name);
+	return 0;
 }
 
 @h Parsing.
diff --git a/services/linguistics-module/Chapter 3/Prepositions.w b/services/linguistics-module/Chapter 3/Prepositions.w
index efcbdda88..12267c7b2 100644
--- a/services/linguistics-module/Chapter 3/Prepositions.w	
+++ b/services/linguistics-module/Chapter 3/Prepositions.w	
@@ -38,9 +38,10 @@ void Prepositions::create_category(void) {
 	METHOD_ADD(prepositions_category, LOG_GRAMMATICAL_CATEGORY_MTID, Prepositions::log_item);
 }
 
-void Prepositions::log_item(grammatical_category *cat, general_pointer data) {
+int Prepositions::log_item(grammatical_category *cat, general_pointer data) {
 	preposition *P = RETRIEVE_POINTER_preposition(data);
 	LOG("%A", &(P->prep_text));
+	return 0;
 }
 
 @ As with verbs, "prepositions" can be long, but are not unlimited.
diff --git a/services/linguistics-module/Chapter 3/Verbs.w b/services/linguistics-module/Chapter 3/Verbs.w
index 61efb1df8..ad13af0b1 100644
--- a/services/linguistics-module/Chapter 3/Verbs.w	
+++ b/services/linguistics-module/Chapter 3/Verbs.w	
@@ -35,9 +35,10 @@ void Verbs::create_category(void) {
 	METHOD_ADD(verbs_category, LOG_GRAMMATICAL_CATEGORY_MTID, Verbs::log_item);
 }
 
-void Verbs::log_item(grammatical_category *cat, general_pointer data) {
+int Verbs::log_item(grammatical_category *cat, general_pointer data) {
 	verb *V = RETRIEVE_POINTER_verb(data);
 	Verbs::log_verb(DL, V);
+	return 0;
 }
 
 verb *Verbs::from_lcon(lcon_ti lcon) {
@@ -177,9 +178,10 @@ void Verbs::create_forms_category(void) {
 	METHOD_ADD(verb_forms_category, LOG_GRAMMATICAL_CATEGORY_MTID, Verbs::log_form_item);
 }
 
-void Verbs::log_form_item(grammatical_category *cat, general_pointer data) {
+int Verbs::log_form_item(grammatical_category *cat, general_pointer data) {
 	verb_form *vf = RETRIEVE_POINTER_verb_form(data);
 	Verbs::log_form(vf);
+	return 0;
 }
 void Verbs::log_form(verb_form *vf) {
 	LOG("$w + $p + $p",
-- 
2.34.1

